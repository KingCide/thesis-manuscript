% !TEX Program = xelatex
\documentclass[print, master, vlined]{DissertUESTC}

\begin{document}
	
	% 以下两条命令为高亮示例文档中的关键内容而设，正式撰写时切不可使用
	\newcommand{\shad}[1]{\textcolor{DodgerBlue}{\ttfamily #1}}
	\newcommand{\shadcmd}[1]{\shad{$\backslash$#1}}

	% \raggedbottom  % 此命令可让LaTeX像Word那样直接堆叠页面内容，而不再默认拉伸段间距，代价是页尾可能会有明显空白
	

	% 封面示例————非双学位学士论文
	%	\uestccover[<学院名称排版风格>]{<论文题目>}{<学科专业>}{<学号>}{<作者姓名>}{<指导教师>}{<教师职称>}{<学院>} % 单学位学士、研究生通用
	\uestccover{面向大规模低轨卫星的在网计算研究}{信息与通信工程}{202321011337}{龚科成}{孙罡}{教\hspace{1em}授}{信息与通信工程学院}  % 空封面
				

	
	
	% 开启中文摘要
	\zhabstract
	
	大规模低轨卫星组成的巨型星座即将成为新一代通信系统的基础设施，它为世界提供给了一种新的连接范式，一方面将全球各地的偏远地区的人相连，另一方面也为大规模全球范围内的物联网提供了新的机遇。
	% 中文关键字
	\zhkeywords{大规模低轨卫星，在网计算，机器学习，物联网}
	
	

	
	
	% 开启英文摘要
	\enabstract
	
	When Yang was a teenager, his mother contracted a disease and 	
	
	% 英文关键字
	\enkeywords{Martial arts, apostasy, revenge, fighting against the enemy, martial arts, apostasy, revenge, fighting against the enemy}

	
	\tableofcontents  % 主目录，必要
%	
%	\listoffigures  % 图多则放，反之不放
%	
%	\listoftables  % 表多则放，反之不放
%	
%	\listofsymbs  % 生成主要符号表标题，需要额外维护符号表内容
		
\chapter{绪论}


\section{研究背景与意义}

\subsection{天地一体化网络的发展趋势与愿景}

随着信息技术的飞速发展和全球互联需求的日益增长，构建一个能够覆盖全球、提供无缝连接的网络体系已成为人类社会发展的战略性目标。传统的地面通信网络，尽管在人口密集区域实现了高度发达的覆盖，但其局限性也日益凸显：全球约有75\%的陆地面积和90\%以上的海洋区域仍是地面蜂窝网络的覆盖盲区。为了弥合这一“数字鸿沟”，实现真正意义上的“万物互联”，学术界与工业界将目光投向了广袤的太空，孕育出了天地一体化信息网络（Space-Terrestrial Integrated Network, STIN）这一宏伟构想。天地一体化信息网络并非简单的网络延伸，而是一个以天基网络为主体、地面网络为基础，能够支持陆、海、空、天各类用户随遇接入、按需服务的革命性信息基础设施。

这一愿景已从概念走向实践，并被提升至国家战略高度。中国在其航天白皮书中明确提出，要建设天地一体化信息网络，基本建成空间基础设施体系，并将其视为维护和拓展国家核心安全利益的关键举措。与此同时，全球范围内的商业航天公司与通信巨头也在积极布局。国际电信联盟（ITU）、欧洲空间局（ESA）等国际组织亦在推动相关技术研究与标准制定，旨在定义卫星通信与地面网络融合的业务交付架构，确保在多域供应环境中提供有保障的服务质量（QoS）与体验质量（QoE）。

天地一体化网络的发展与下一代移动通信技术，即第六代移动通信（6G）的演进紧密相连。如果说第五代移动通信（5G）通过其非地面网络（Non-Terrestrial Network, NTN）标准，初步探索了卫星等非地面网络与地面网络的协同工作模式，那么6G则致力于实现二者更深层次的无缝融合。在6G的蓝图中，卫星网络不再仅仅是地面网络的补充或备用链路，而是作为一个内生的、有机的组成部分，共同构成一个异构、立体、智能化的全球网络。这一融合网络旨在实现超高速率、超低延迟和超大连接数的目标，支持全息通信、数字孪生、全球物联网等前沿应用，最终达成“全球覆盖、随遇接入、按需服务、安全可信”的终极愿景。这种从“协同”到“融合”的范式转变，标志着天地一体化网络正从一个补充性技术方案，演进为未来全球信息社会不可或缺的核心基础设施。

\subsection{卫星物联网 (Satellite IoT) 数据激增带来的挑战}

天地一体化网络愿景的实现，极大地催生了卫星物联网（Satellite Internet of Things, SIoT）市场的蓬勃发展。卫星物联网利用卫星通信技术，为地面网络无法覆盖的偏远地区（如海洋、沙漠、山脉）和特殊行业（如农业、物流、海事、应急管理）的物联网设备提供连接服务，是实现全球物联网无缝覆盖的关键拼图。近年来，一场深刻的技术变革正在重塑卫星产业的成本结构，从而引爆了卫星物联网市场的指数级增长。这场变革的核心，是从传统上依赖于部署在地球静止轨道（Geostationary Orbit, GEO）的、重达数吨、造价高昂的大型卫星，转向在低地球轨道（Low Earth Orbit, LEO）部署由成百上千颗小型化、模块化、标准化生产的卫星组成的巨型星座。这一转变，加之可重复使用运载火箭技术带来的发射成本大幅下降，使得卫星连接的单位成本显著降低，从而将卫星物联网的应用门槛推向了前所未有的低点。

成本的下降直接驱动了市场的爆炸性扩张。根据多家市场研究机构的预测，全球卫星物联网市场正经历“爆炸式”增长阶段。IoT Analytics的报告显示，全球卫星物联网连接数在2024年已达到750万，并预测到2030年，相关市场规模将超过47亿美元，年复合增长率（CAGR）高达26\%。Berg Insight的预测更为激进，预计全球卫星物联网用户数将从2023年的510万飙升至2028年的2670万，复合年增长率接近40\%。这一增长趋势得到了业界的广泛共识，它预示着在不久的将来，将有数千万甚至上亿的物联网设备通过卫星接入网络，产生海量的传感、监控和遥测数据。

然而，这种由技术进步和成本下降驱动的良性循环，正催生出一个严峻的挑战：数据激增与星地链路（Satellite-to-Ground Link）带宽瓶颈之间的深刻矛盾。卫星物联网的成功部署，意味着全球范围内数以亿计的传感器将以前所未有的规模生成数据。这些数据，尽管单个数据包可能很小，但其汇聚而成的总量将是惊人的。据预测，到2025年，全球每天产生的数据量将达到463艾字节（Exabytes）。卫星物联网将是这一数据洪流的重要来源之一。然而，卫星与地面站之间的通信链路，受限于有限的频谱资源、高昂的建设成本以及物理定律，其总带宽是有限且宝贵的。当海量的原始物联网数据，包括大量冗余、低价值甚至错误的数据，都试图通过这一“窄门”回传至地面数据中心进行处理时，必然会导致严重的网络拥塞、传输延迟和服务质量下降。这种潜在的架构性危机，即网络的成功应用反过来造成了其自身基础设施的拥塞，正成为制约卫星物联网可持续发展的核心瓶颈。因此，如何高效地管理和优化这一海量数据流，在数据离开卫星之前进行有效的预处理，已成为该领域亟待解决的关键科学问题。

\subsection{LEO卫星网络资源受限的本质}

低地球轨道（LEO）卫星网络凭借其低延迟、低损耗的优势，成为构建下一代天地一体化网络和卫星物联网的核心技术。然而，与地面数据中心或网络设备相比，LEO卫星本质上是一个资源极度受限的平台。这种限制并非偶然，而是由空间环境的物理定律和航天工程的经济学原理共同决定的，其核心可以归结为对尺寸、重量和功率（Size, Weight, and Power, SWaP）的严苛约束。

发射成本是制约卫星设计的根本因素。将一公斤有效载荷送入LEO轨道仍需数千美元的成本，这迫使卫星设计者必须在功能与重量之间做出艰难的权衡，每一克重量都需精打细算。这种对重量的极致追求，直接限制了卫星上所有部件的规模和复杂性。其次，功率是卫星需要主要考量的因素。LEO卫星的主要能源来自太阳能帆板，其面积受限于卫星的整体尺寸（例如，立方星CubeSat的标准化尺寸）和发射整流罩的空间。有限的帆板面积意味着有限的发电功率，这一功率预算必须在通信载荷、计算单元、姿态控制系统和热管理系统等多个耗电大户之间进行分配。典型的LEO卫星最大发射功率可能仅为100瓦左右，远低于地面设备。

在这样严苛的SWaP约束下，卫星的各项资源都受到了显著限制。在计算资源方面，高性能的计算芯片通常功耗巨大，且对散热要求高。因此，星上处理器通常选用经过特殊设计、具备高能效比和抗辐射能力的宇航级CPU或FPGA。这些芯片的计算能力往往落后于地面商用芯片数代，其处理能力有限。例如，相关研究中的仿真参数显示，单颗LEO卫星的计算资源可能在3×10\textsuperscript{10} cycle/s的量级，显著低于地面云服务器的1×10\textsuperscript{11} cycle/s。在通信资源方面，频谱是稀缺且受到严格管制的资源。同时，天线的尺寸和发射功率直接影响通信带宽和信号质量，而这两者同样受到SWaP的严格限制。学术研究中，LEO网络的可用链路带宽通常被设定在几十兆赫兹的范围内，如25 MHz。此外，存储资源也同样受限，卫星无法像地面数据中心那样拥有海量的存储空间。

综上所述，LEO卫星的资源受限并非一个可以通过简单堆砌硬件来解决的工程问题，而是由发射经济学和轨道物理学决定的固有属性。这种计算、通信、存储和功率资源的全面受限，与前述卫星物联网的海量数据处理需求之间，形成了尖锐的矛盾。这一矛盾凸显了在卫星上采用轻量级、高效率的软件和算法方案，以“四两拨千斤”的方式提升系统整体性能的极端重要性和紧迫性。

\subsection{在网计算赋能星上边缘处理的研究意义}

面对卫星物联网数据激增与LEO卫星资源受限的双重挑战，传统的卫星网络架构显得力不从心。传统的“透明转发”（Bent-pipe）架构将卫星视为一个简单的空中中继器，其唯一功能是将地面终端的信号接收、放大并转发至地面站，所有的数据处理和智能分析都在地面数据中心完成。这种模式虽然技术成熟、卫星设计简单，但在数据量爆炸的今天，它将海量的原始数据，包括其中大量的冗余、错误和低价值信息，不加区分地挤占宝贵的星地链路带宽，造成了巨大的资源浪费和网络拥塞。

为了打破这一困局，将计算能力推向网络边缘，在数据源头进行处理的边缘计算（Edge Computing）思想被引入到卫星网络领域，催生了“星上边缘计算”（Satellite Edge Computing）或“轨道边缘计算”（Orbital Edge Computing）这一新兴范式。其核心思想是将卫星从一个“哑管道”转变为一个智能的“边缘节点”，赋予其在轨进行数据处理、分析和决策的能力。在这一范式下，一种更具革命性的技术——在网计算（In-Network Computing, INC），展现出巨大的潜力。在网计算利用可编程网络设备（如P4交换机）的数据平面，在数据包转发的路径上，以线速（line-rate）执行计算任务，实现了计算与网络的深度融合。

将在网计算技术应用于星上边缘处理，意味着我们可以将卫星内部的交换芯片从一个单纯的“路由器”升级为一个高效的“数据预处理器”。其研究意义重大且深远。首先，最直接的意义在于极大地缓解星地链路的带宽压力。通过在卫星上部署轻量级的数据去重、压缩、过滤等在网计算任务，可以在数据回传前剔除大量冗余信息，只将高价值、经过提炼的结果传回地面。中国的“天算星座”计划已经通过星地协同计算的初步验证，实现了回传数据量减少90\%的惊人效果，这充分证明了该路径的巨大潜力。其次，在网计算能够显著降低业务延迟。对于海洋监测、灾害预警等时间敏感型应用，将数据分析任务从地面数据中心前移至卫星，可以免去长距离传输的往返时延，实现近乎实时的响应。再者，它能有效提升数据安全与隐私保护水平。敏感数据无需在开放的地面网络中传输，在轨处理后即可丢弃原始数据，大大降低了数据泄露的风险。

最终，在网计算赋能的星上边缘处理，是从根本上解决卫星物联网规模化发展瓶颈的关键技术路径。它通过将计算负载智能地分布到网络边缘，优化了整个系统的资源利用效率，有望使构建一个经济、高效、智能、安全的全球卫星物联网成为可能。因此，研究面向LEO卫星物联网场景、基于在网计算的星上数据预处理机制，不仅具有重要的理论价值，更对推动天地一体化网络和6G技术的落地应用具有不可估量的实践意义。

\section{国内外研究现状}

\subsection{LEO卫星网络技术研究现状}

近年来，随着SpaceX的Starlink、OneWeb、Amazon的Kuiper等巨型星座计划的推进，LEO卫星网络技术的研究进入了一个新的黄金时代，呈现出理论与实践并进的繁荣景象。当前的研究现状主要围绕网络架构、标准化进程、以及关键技术挑战展开。

在网络架构方面，现代LEO卫星网络正从早期的单点通信模式，向着构建一个具有强大在轨处理与路由能力的太空互联网（Internet in Space）演进。其核心特征是大规模采用星间链路（Inter-Satellite Links, ISLs），特别是高速激光星间链路。通过ISL，轨道上的卫星可以构建成一个动态的、高带宽的网状网络（Mesh Network），数据可以在卫星之间进行多跳转发，从而实现全球范围内的低延迟通信，减少对地面站的依赖。这种架构使得网络流量可以“绕行”拥堵的地面网络，为偏远地区间的通信提供更优的路径。Starlink的实践表明，其网络会以15秒为周期进行全局性的网络重构，动态调整用户与卫星、卫星与地面站之间的连接路径，以适应卫星的快速移动和实现负载均衡，这种高度动态性是当前LEO网络架构研究的核心特征之一。

在标准化进程方面，为了实现卫星网络与地面移动通信网络的无缝融合，各大标准组织，特别是第三代合作伙伴计划（3GPP），正在积极推进非地面网络（NTN）的标准化工作。自Release-15开始进行场景和信道模型研究以来，3GPP在Release-17中正式完成了支持NTN的首个规范版本，标志着卫星通信被正式纳入5G生态系统。Release-17主要聚焦于采用透明转发（Transparent Payload）模式的卫星，即卫星仅作为射频信号的中继，而基站（gNB）功能仍在地面，这种方式对现有5G NR协议的改动最小。其研究内容涵盖了为适应卫星通信长时延、大频偏等问题而进行的物理层和高层协议增强，以及移动性管理、服务质量（QoS）保障等关键系统问题。随后的Release-18及未来的6G研究，将进一步探索更复杂的场景，如具有在轨再生处理能力（Regenerative Payload）的卫星、星上基站、以及更高效的移动性和切换管理机制，旨在让卫星接入成为与地面Wi-Fi、蜂窝网络同等地位的一种标准接入方式。

在关键技术挑战的研究上，学术界正从多个维度展开探索。动态路由是LEO网络研究的核心难点之一，由于拓扑的周期性剧烈变化，传统的地面网络路由协议（如OSPF）难以直接适用，研究人员正在探索基于时间演化图、或利用拓扑可预测性进行改进的路由算法。移动性管理是另一大挑战，包括小区级的移动性（波束切换）和卫星级的移动性（卫星切换），如何实现无缝、低延迟的切换是保障用户体验的关键。网络安全问题也日益受到重视，LEO网络广阔的覆盖范围和广播特性使其容易成为DDoS攻击等网络攻击的目标，研究如何在这种动态、资源受限的环境下设计高效的安全防护机制是一个活跃的研究方向。此外，由卫星高速运动引起的多普勒频移补偿、信道建模、以及面向物联网场景的随机接入协议优化等问题，也是当前LEO网络技术研究的热点。

\subsection{在网计算与P4可编程网络技术研究现状}

在网计算（In-Network Computing, INC）作为一种新兴的计算范式，旨在将计算任务从终端设备或中心云，卸载到网络路径中的交换机、路由器等转发设备上执行，其发展与数据平面可编程技术的演进密不可分。这一领域的研究现状，标志着网络从一个被动的数据管道，向一个主动的、可编程的计算平台转变。

研究的起点可以追溯到软件定义网络（Software-Defined Networking, SDN）的出现。SDN通过解耦控制平面与数据平面，首次实现了对网络行为的集中式可编程控制。其代表性协议OpenFlow允许控制平面动态下发流表规则，指导数据平面进行“匹配-动作”（Match-Action）处理。然而，OpenFlow的局限性也十分明显：它所能匹配的协议头字段集合是固定的，由标准预先定义。当需要处理新的协议或自定义协议头时，必须等待标准更新和硬件厂商支持，这极大地束缚了数据平面的创新速度和灵活性。

为了突破OpenFlow的桎梏，P4（Programming Protocol-independent Packet Processors）语言应运而生，并迅速成为数据平面可编程领域的事实标准。P4的革命性在于它实现了三个核心目标：协议无关性、现场可重构性和目标可移植性。协议无关性意味着网络程序员可以自行定义数据包的解析逻辑，让交换机能够识别和处理任意格式的协议头，而不再受限于厂商的硬编码实现。这种“自顶向下”的设计范式，将数据平面的定义权从硬件厂商交还给了网络运营者和研究者，极大地加速了网络功能的创新周期。

基于P4的在网计算已经在地面网络，特别是数据中心网络中，展现出巨大的应用价值。当前的研究主要集中在以下几个方面：首先是网络遥测与监控。通过P4实现的带内网络遥测（In-band Network Telemetry, INT）技术，可以将交换机内部的状态信息（如队列深度、处理时延）直接编码到数据包中，随包传输，从而实现对网络状态的线速、全路径、高精度可见性。其次是流量工程与优化。利用P4的灵活性，研究人员设计并实现了多种高效的负载均衡、拥塞控制和主动队列管理（AQM）算法，这些算法可以直接在数据平面运行，实现微秒级的快速响应。再次是网络安全。P4交换机被用于构建高性能的DDoS攻击缓解系统和可编程防火墙，它们能够在网络入口处以线速检测并过滤恶意流量，远比传统的基于CPU的方案高效。此外，一些更前沿的研究开始探索利用P4数据平面来加速分布式系统应用，如分布式共识协议、键值存储和机器学习推理等。

为了支撑P4的研发与应用，一个完整的生态系统已经形成，包括了官方的P4语言规范、开源的编译器后端（如p4c）、行为级软件交换机模拟器（如BMv2）、以及与网络仿真平台（如Mininet）的集成工具，这些都极大地降低了研究和开发P4应用的门槛。总而言之，P4和在网计算技术已经从一个学术概念发展成为一个拥有坚实理论基础、丰富应用场景和完善工具链的成熟研究领域。

\subsection{物联网数据聚合与预处理技术研究现状}

物联网（IoT）场景下，海量设备产生的原始数据往往具有高度的冗余性、时空相关性和价值稀疏性。直接传输和存储所有原始数据会造成巨大的通信带宽和存储资源浪费。因此，在数据传输路径上对数据进行聚合与预处理，是物联网系统设计的关键环节。当前，该领域的研究主要聚焦于设计轻量级的算法，以适应物联网终端和边缘节点资源受限的特点。

数据聚合技术旨在通过融合来自多个传感器的数据来消除冗余传输，从而节约能源和带宽。根据实现方式，聚合技术可分为多种。一种是基于树状或簇状的层次化聚合，网络中的节点被组织成簇，簇内数据先汇聚到簇头节点，再由簇头节点进行聚合后向上传输，这种方式可以有效减少传输跳数和数据总量。另一种是数据中心路由（Data-centric Routing），其中数据本身的内容（如事件类型）决定了其路由路径，典型的例子是“定向扩散”（Directed Diffusion），汇聚节点（Sink）通过发布兴趣来“拉取”相关数据，中间节点可以对数据进行缓存和融合。这些聚合方法的核心目标都是在保证信息质量的前提下，最大化网络生命周期。

数据预处理技术则更侧重于对数据内容本身进行清洗和精炼。其中，数据去重（Deduplication）是研究的热点之一。由于物联网设备部署密集，相邻设备在同一时刻采集到的数据往往高度相似或完全相同。去重技术通过识别并消除这些重复数据来节省资源。现有技术主要包括：基于哈希的去重，通过计算数据块的哈希值（如SHA-1）来判断其是否重复，这是最常用的方法。为了处理相似但不完全相同的数据，研究人员提出了相似哈希（SimHash）或基于内容分块（Content-Defined Chunking）结合布隆过滤器（Bloom Filter）的方案，以识别相似数据。此外，为了在去重的同时保护数据隐私，基于同态加密或安全多方计算的加密去重方案也被提出，但其计算开销较大。一些研究还利用默克尔树（Merkle Tree）或其变体（如扩展默克尔网格）来高效地进行数据校验和去重验证。

除了去重，数据过滤也是重要的预处理手段。这包括滤除明显的异常值或由传感器故障产生的错误数据。近年来，随着人工智能技术的发展，轻量级的机器学习模型被用于物联网边缘节点，进行更智能的数据预处理。例如，利用决策树、支持向量机（SVM）或轻量级的深度学习模型（如1D-CNN）在边缘端进行数据分类或异常检测，只将有意义的事件或异常模式上传，从而大幅减少数据量。联邦学习（Federated Learning）作为一种分布式机器学习范式，也为物联网数据处理提供了新思路，它允许在不上传原始数据的情况下，在各设备上进行本地模型训练，仅聚合模型参数，兼顾了效率与隐私。

总的来看，物联网数据聚合与预处理技术的研究已经相当深入，形成了丰富的算法库。然而，这些研究的一个共同特点是，其算法实现大多是面向通用的计算平台，如物联网设备内置的微控制器（MCU）或边缘服务器的CPU。它们的设计假设了一个可以执行复杂逻辑、循环、分支和拥有灵活内存访问的冯·诺依曼计算模型。

\subsection{现状评述与研究切入点分析}

通过对LEO卫星网络、在网计算与P4技术、以及物联网数据预处理技术这三个相关领域研究现状的梳理，可以清晰地看到一幅技术发展的宏观图景：一方面，LEO卫星网络正在成为承载全球物联网的关键基础设施，但其面临着由数据激增和自身资源受限所导致的严峻挑战；另一方面，P4可编程网络和在网计算技术为在网络内部高效处理数据提供了前所未有的强大工具，并在地面网络中取得了巨大成功；同时，针对物联网数据的轻量级聚合与预处理算法也已发展得相当成熟。然而，当我们将这三个领域进行交叉审视时，一个明显的研究空白与机遇便浮现出来。

现有研究呈现出一种“割裂”的状态。关于LEO网络的研究，大多集中在物理层、链路层和网络层的协议设计与优化上，如路由、移动性管理和信道接入，较少深入探讨如何利用网络层以上的能力对承载的数据内容进行智能处理。关于P4在网计算的研究，其应用场景几乎全部局限于高带宽、资源相对充裕、拓扑稳定的地面数据中心或企业网，鲜有工作将其置于LEO卫星网络这种高动态、资源极度受限的极端环境中进行考量。而关于物联网数据预处理算法的研究，其设计和实现完全基于传统的CPU计算模型，未曾考虑过如何将其适配到P4所代表的、高度受限的流水线式数据平面编程模型中。

正是这三个领域的交叉点，构成了本论文的核心研究切入点。将P4在网计算技术应用于LEO卫星网络，以解决卫星物联网的数据预处理问题，是一个极具前瞻性但又充满挑战的课题。现有技术无法直接迁移，必须进行根本性的创新。具体而言，本研究旨在填补以下关键空白：

首先，\textbf{算法层面的创新}。现有的物联网去重、过滤算法，无论是基于复杂的密码学操作，还是基于迭代式的机器学习模型，都无法直接在P4数据平面中实现。P4的流水线架构不支持循环，状态存储（寄存器、计数器）资源极为有限，且可执行的算术逻辑单元（ALU）操作非常基础。因此，本研究的第一个切入点是：如何重新设计数据预处理算法，使其逻辑能够被“翻译”成P4的“匹配-动作”表（Match-Action Table）流水线？这要求算法必须是“单遍”（single-pass）的，并且对状态存储和计算复杂度的要求极低。

其次，\textbf{系统层面的挑战}。即便设计出P4兼容的算法，如何在一个高速运动、拓扑每时每刻都在变化的LEO网络中有效管理其所需的状态，是另一个巨大的挑战。例如，一个在网去重应用需要在P4交换机中维护一个已见数据包哈希值的表。当卫星网络发生切换或重构时（如Starlink的15秒周期性重构），这个状态表是应该被丢弃、迁移还是与其他卫星同步？状态的生命周期管理、一致性保证以及在动态网络中的高效同步，是现有P4应用从未遇到过的问题。这是本研究的第二个切入点。

综上所述，本论文的研究切入点在于探索上述三个领域的深度融合，致力于回答一个核心问题：如何设计一个面向LEO卫星物联网场景的、基于P4在网计算的轻量级数据预处理框架，并解决其中的关键算法设计与动态状态管理难题。这项研究不仅有望为解决卫星物联网的带宽瓶颈问题提供一个全新的、高效的解决方案，也将极大地拓展P4在网计算的应用边界，推动其从地面网络走向广阔的空天领域。

\section{本文主要研究内容}

基于上述研究背景、意义及切入点分析，本文将围绕在LEO卫星网络中利用在网计算技术对物联网数据进行高效预处理这一核心目标，展开以下两个层面的主要研究内容。

\subsection{星上轻量级数据预处理机制设计}

本部分内容聚焦于算法与机制的顶层设计，旨在解决传统物联网数据处理算法与P4数据平面编程模型不兼容的核心矛盾。研究将从卫星物联网数据的典型特征（如数据冗余、格式多样性）出发，探索能够在P4交换机严苛的资源限制下高效执行的数据预处理操作。主要研究内容包括：

第一，\textbf{在网数据去重算法设计}。针对物联网传感器数据中普遍存在的大量重复信息，研究将设计一种或多种轻量级的在网去重算法。这需要摒弃传统软件实现中复杂的逻辑，将去重过程分解为P4流水线能够执行的基本操作。研究将探索如何利用P4的哈希计算、寄存器阵列和布隆过滤器等原生能力，构建一个内存占用小、匹配速度快的去重状态表，并设计相应的匹配-动作逻辑，以线速识别并丢弃重复的数据包。

第二，\textbf{在网数据过滤机制研究}。除了完全重复的数据，物联网数据流中还包含大量根据特定规则可以被过滤的“无用”数据（例如，超出正常阈值的异常读数、不符合特定格式的数据包等）。本研究将设计一种灵活的在网过滤机制，允许网络管理员通过控制平面动态配置过滤规则。这些规则将被编译成P4的匹配表项，使得卫星上的P4交换机能够根据数据包的头部或载荷内容，实时地对数据流进行筛选。

第三，\textbf{面向异构物联网数据的可编程解析}。物联网协议种类繁多，标准不一。本研究将利用P4的协议无关性，设计一个可编程的解析器，使其能够根据数据包的特征动态识别并解析不同类型的物联网数据格式，从而为后续的去重和过滤操作提取出关键的元数据字段。

\subsection{面向异构任务的星上计算卸载与资源调度策略}

在设计了具体的预处理算法之后，本部分内容将从系统层面出发，研究如何在一个动态的、多任务的星上环境中，对这些在网计算任务进行有效的资源分配和管理。这部分研究旨在解决星上计算资源稀缺性和网络拓扑高动态性带来的挑战。主要研究内容包括：

第一，\textbf{P4交换机资源感知与建模}。为了实现高效的资源调度，首先需要对P4交换机的内部资源（如匹配表项、寄存器内存、ALU单元等）进行精确的感知和建模。研究将分析P4程序编译后对硬件资源的占用情况，建立一个能够量化不同在网计算任务资源消耗的模型，为调度决策提供依据。

第二，\textbf{动态网络环境下的状态管理策略}。这是本研究的核心难点之一。针对LEO网络拓扑的周期性变化和频繁切换，研究将探索不同的在网状态管理策略。例如，对于去重应用所需的状态哈希表，是采用“无状态”设计（切换后状态即失效），还是设计一种轻量级的状态同步或迁移机制，在卫星切换时将关键状态信息传递给下一个服务卫星。研究将分析不同策略在开销、一致性和处理效率之间的权衡。

第三，\textbf{异构任务联合调度与卸载决策}。星上平台可能需要同时承载多种数据预处理任务（如去重、过滤、聚合等），以及其他网络功能。这些任务对P4资源的竞争关系需要被妥善管理。本研究将探索一种联合调度策略，根据任务的优先级、数据流的特征以及当前卫星的可用资源，动态地决定哪些任务被加载到P4流水线中，以及如何为它们分配合适比例的硬件资源，以实现系统整体效用的最大化。

\section{本文主要贡献与创新}

本论文旨在通过将前沿的P4在网计算技术创造性地应用于LEO卫星物联网这一新兴领域，解决其面临的数据爆炸与链路瓶颈的根本性矛盾。本文的主要贡献与创新点可以概括为以下四个方面：

\subsection{提出一种面向LEO卫星物联网场景的、基于在网计算的轻量级数据预处理框架}

本文的第一个核心创新在于，首次系统性地提出了一个将数据预处理功能深度集成到LEO卫星网络数据平面的全新框架。该框架颠覆了传统卫星作为“透明管道”的设计理念，将卫星改造为一个具备智能处理能力的边缘计算节点。与传统的星上边缘计算方案大多依赖于通用CPU进行软件处理不同，本框架利用P4可编程交换芯片的线速处理能力，将数据预处理任务下沉至网络转发路径本身，实现了计算与通信的深度融合。这一框架性创新为解决LEO卫星网络资源受限与海量物联网数据处理需求之间的矛盾提供了一个全新的、高效的解决思路。

\subsection{针对P4交换机资源限制，设计并实现了高效的在网去重与过滤算法}

本文的第二个主要创新点体现在算法层面。深刻认识到通用计算平台上的算法无法直接移植到P4数据平面，本文针对P4流水线架构的特点和资源限制（如无循环、有限状态、简单操作），创造性地设计了一套轻量级的在网数据预处理算法。特别是在数据去重方面，本文将探索并实现一种或多种P4原生的去重算法，它们能够高效利用P4的哈希、寄存器和匹配-动作原语，在满足线速处理要求的同时，最大限度地降低对星上宝贵存储和计算资源的占用。这些算法的提出，填补了现有物联网数据处理技术与P4可编程数据平面之间的理论与实践鸿𝐠𝐚𝐩。

\subsection{探索了LEO网络高动态性下的在网状态管理策略}

本文的第三个关键创新在于，直面并探索了在网计算应用于LEO卫星网络时最独特的挑战——动态状态管理。传统的在网计算应用场景（如数据中心）拓扑相对稳定，而LEO网络的高速移动和周期性重构使得状态管理变得异常复杂。本文将首次对这一问题进行系统性研究，提出并分析多种可能的在网状态管理策略，如无状态设计、周期性状态重置、基于星间链路的轻量级状态迁移等。对这些策略在网络开销、数据处理一致性和实现复杂度等方面的权衡进行深入分析，为在动态网络环境中部署有状态的在网计算应用提供了宝贵的理论依据和实践指导。

\subsection{通过仿真实验验证了所提机制的有效性}

为确保研究的科学性和严谨性，本文的第四个贡献在于构建了一个综合性的仿真实验平台，对所提出的框架、算法和策略进行全面的性能评估。该平台将结合网络仿真工具（如Mininet或其变体）和P4行为级模拟器（BMv2），在一个能够模拟LEO网络拓扑动态变化、卫星资源限制和卫星物联网流量特征的环境中，对本文提出的机制进行端到端的验证。通过与传统的“透明转发”模式以及基于CPU的星上处理模式进行对比，本文将用定量的实验数据，从数据压缩率、端到端延迟、资源占用率等多个维度，有力地证明所提方案的有效性和优越性。

\section{论文结构安排}

本文的组织结构如下：

第一章：绪论。介绍研究背景与意义，阐述天地一体化网络的发展趋势、卫星物联网数据激增的挑战以及LEO卫星网络资源受限的本质，论证在网计算赋能星上边缘处理的研究价值。随后，对国内外相关领域的研究现状进行综述和评述，明确本研究的切入点。最后，概括本文的主要研究内容、贡献与创新点，并给出全文的结构安排。

第二章：相关概念与技术基础。详细介绍本研究涉及的核心概念和基础技术。首先阐述LEO卫星网络与物联网的融合，包括LEO网络体系架构、卫星物联网数据流特征及高动态性挑战。接着介绍在网计算技术，特别是P4可编程数据平面技术及其典型应用。最后，深入分析在LEO卫星网络中应用在网计算的机遇与挑战。

第三章：面向LEO卫星物联网的在网数据预处理框架。本章将详细阐述本文提出的核心框架。首先给出框架的总体设计，明确各功能模块及其交互关系。然后，重点介绍框架中的关键机制，包括可编程的异构数据解析、基于P4的在网去重与过滤的实现原理。

第四章：星上在网计算的状态管理与资源调度策略。本章聚焦于解决动态性和资源受限两大挑战。首先，针对LEO网络的高动态性，设计并分析多种在网状态管理策略。其次，针对星上P4交换机资源受限问题，提出一种面向异构预处理任务的资源感知与联合调度模型。

第五章：仿真实验与性能评估。本章将通过仿真实验对本文提出的理论和方法进行验证。首先介绍仿真环境的搭建，包括LEO网络拓扑的构建、卫星物联网流量模型的生成以及P4仿真平台的配置。然后，设计多组对比实验，从数据压缩率、网络延迟、吞吐量、资源开销等多个性能指标，对本文提出的框架和算法进行全面评估和分析。

第六章：总结与展望。对全文的研究工作进行总结，重申本文的主要贡献和创新。同时，指出当前研究存在的不足之处，并对未来可能的研究方向进行展望。

\chapter{相关概念与技术基础}

本章旨在为后续章节的深入论述奠定坚实的理论基础。首先，将详细介绍低地球轨道（LEO）卫星网络与物联网融合的背景，阐明其系统架构、数据特征以及核心挑战。其次，将深入剖析在网计算（In-Network Computing）这一关键使能技术，重点介绍作为其实现基础的P4可编程数据平面技术。最后，将综合前两部分内容，系统性地分析在LEO卫星网络这一特定场景中部署在网计算所面临的独特机遇与严峻挑战，从而为本文的研究工作提供清晰的技术图景。

\section{LEO卫星网络与物联网融合}

\subsection{LEO卫星网络体系架构}

LEO卫星网络作为一种新兴的通信基础设施，其体系架构与传统的地面网络或其他轨道（如GEO、MEO）的卫星系统均有显著不同。一个典型的LEO卫星通信系统，从功能上可以划分为三个相互协作的核心部分：空间段（Space Segment）、地面段（Ground Segment）和用户段（User Segment）。这三部分共同构成了一个完整的、端到端的通信链路，为全球用户提供服务。

空间段是LEO网络的核心，由部署在近地轨道（通常高度在500公里至2000公里之间）的成百上千颗卫星组成的星座构成。与看起来静止不动的GEO卫星不同，LEO卫星以极高的速度（约7.6 km/s）环绕地球运行，单颗卫星对地面某点的可见时间仅为几分钟。为了提供连续覆盖，必须依赖大规模的卫星星座。现代LEO星座的一个关键技术是星间链路（ISL），特别是高速、大容量的激光星间链路。通过ISL，相邻的卫星可以直接通信，形成一个在太空中动态连接的网状网络。这种“太空骨干网”允许数据在卫星之间进行多跳转发，极大地降低了对地面中继站的依赖，实现了真正意义上的全球路由和低延迟通信。根据其在轨处理能力，卫星载荷可分为“透明转发”（Transparent Payload）和“再生处理”（Regenerative Payload）两种。透明载荷仅对信号进行频率转换和放大，如同一个简单的“弯管”（Bent-pipe），所有基带处理和路由决策都在地面完成，这是3GPP Release-17 NTN标准初期的主要模式。再生载荷则在卫星上集成了基带处理、解调/编码、交换路由等功能，相当于将一个地面基站或路由器搬到了太空，具备更强的在轨处理能力和灵活性，是未来发展的趋势。

地面段是连接空间段与地面互联网的桥梁，主要包括关口站（Gateway Station）、测控站（Telemetry, Tracking, and Command, TT\&C Station）和网络操作中心（Network Operations Center, NOC）。关口站是大型地面站，通过大口径天线与卫星星座连接，负责将来自卫星的业务流量接入地面核心网（如互联网、移动核心网），以及将地面流量上传至卫星。测控站则负责跟踪卫星的轨道、姿态，发送控制指令，并接收卫星回报的遥测数据，以确保卫星的正常运行。NOC是整个星座的大脑，负责对网络资源进行集中管理、调度和监控，包括路由计算、频谱分配、用户管理等。

用户段涵盖了所有接入LEO卫星网络的终端设备。这些终端的形态多种多样，以适应不同的应用场景。对于宽带互联网接入服务，用户终端通常是小型的、具备自动跟踪卫星能力的相控阵天线，如Starlink的用户终端（Dishy）。对于移动通信，未来的智能手机将能够直接连接卫星（Direct-to-Cell）。而对于本研究聚焦的物联网应用，用户段则由海量的、通常是低功耗、低成本的物联网终端构成，它们可能分布在农业、物流、环境监测等各种场景中，通过卫星实现数据的采集与回传。

为了更具体地理解现代LEO巨型星座的规模与特征，下表对当前主流的几个系统进行了对比。

\begin{table}[htbp]
    \centering
    \caption{主流LEO巨型星座体系架构对比 (数据来源: 公开资料)}
    \begin{tabular}{llll}
        \toprule
        \textbf{特征} & \textbf{SpaceX Starlink} & \textbf{OneWeb} & \textbf{Amazon Kuiper} \\
        \midrule
        \textbf{轨道高度} & \textasciitilde550 km & \textasciitilde1200 km & \textasciitilde590-630 km \\
        \textbf{计划卫星数量} & \textasciitilde12,000+ & \textasciitilde648 (第一阶段) & \textasciitilde3,236 \\
        \textbf{星间链路技术} & 激光链路 (V1.5及以后版本) & 初期无，后续计划 & 激光链路 \\
        \textbf{用户终端带宽 (下行)} & 25-220 Mbps & 高达 195 Mbps & 高达 400 Mbps \\
        \textbf{用户终端带宽 (上行)} & 5-25 Mbps & 高达 32 Mbps & 未公布 \\
        \textbf{在轨处理架构} & 再生处理能力不断增强 & 初期为透明转发 & 具备在轨处理能力 \\
        \bottomrule
    \end{tabular}
    \label{tab:leo_constellations}
\end{table}

此表清晰地揭示了当前LEO网络发展的共同趋势：大规模部署、低轨道运行以保证低延迟、以及普遍采用星间链路构建太空网络。这些架构特征，特别是星间链路的普及和在轨处理能力的引入，为在网络内部署计算任务提供了物理基础和可能性。

\subsection{卫星物联网的数据流特征}

卫星物联网（SIoT）所承载的数据流，在特性上与传统的人与人（Human-to-Human）或人与机器（Human-to-Machine）通信有着本质的区别。准确理解其数据流特征，是设计高效网络协议和处理机制的前提。SIoT的流量模式通常被归类为海量机器类型通信（massive Machine Type Communications, mMTC），其核心特征可以概括为：海量连接、数据突发、包长极短。

首先，\textbf{海量连接}（Massive Connectivity）是其最显著的特征。与服务于人类用户的宽带网络不同，SIoT旨在为数以千万计甚至亿计的设备提供连接。这些设备（如环境传感器、资产追踪器、智能农具）可能在广阔的地理范围内密集部署。如此庞大的终端数量，对网络的接入控制、地址管理和信令系统构成了前所未有的压力。传统的为每个连接进行握手和资源分配的模式，在如此巨大的连接规模下会产生难以承受的信令开销。

其次，\textbf{数据突发}（Sporadic/Bursty Traffic）是其典型的行为模式。大多数物联网设备并非持续不断地发送数据，而是在大部分时间处于休眠或静默状态以节省能量，仅在特定事件触发时（如检测到异常、达到预设时间点）才被激活，并发送一个或一小批数据。这种流量模式导致信道负载在时间和空间上极不均衡：可能长时间空闲，又在某个瞬间出现大量设备同时尝试接入，形成“信令风暴”或“接入拥塞”，对网络的随机接入信道（RACH）造成巨大冲击。

再次，\textbf{包长极短}（Short Packets）是其数据的普遍形态。物联网应用的数据传输通常是为了上报状态、位置或一个简单的测量值，因此数据包的有效载荷非常小，可能只有几十或几百个字节。在这种情况下，传统网络协议（如TCP/IP）的协议头开销占比会非常高，导致传输效率低下。例如，一个40字节的TCP/IP协议头去承载一个20字节的传感器数据，其有效数据传输效率不足34\%。这种“头重脚轻”的现象在SIoT中极为普遍。

这些数据流特征共同决定了SIoT网络面临的核心挑战，并非源于单个用户的高带宽需求，而是源于\textbf{海量、非协调、突发性短包接入所引发的信令与媒体接入控制（MAC）层面的危机}。传统的基于竞争的接入协议，如ALOHA及其变体，虽然机制简单，但在高并发接入时会因严重的包碰撞而导致信道吞吐量急剧下降，即所谓的“拥塞崩溃”。而基于请求/授权的协议，虽然能避免碰撞，但其为每个短包进行资源申请和分配所带来的信令交互和等待时延，对于突发性业务而言又显得过于笨重和低效。因此，SIoT的数据流特性，强烈地指向了一种技术需求：在网络接入的第一个汇聚点，即LEO卫星上，对数据进行有效的预处理。通过聚合多个短包、过滤冗余数据，可以极大地优化后续的传输和处理流程，从根本上缓解由SIoT独特流量模式带来的网络压力。

\subsection{LEO网络的高动态性挑战}

LEO卫星网络最显著也最具挑战性的特征，源于其组成单元——卫星——的永恒运动。与固定在地面的基站或静止在太空的GEO卫星不同，LEO卫星以大约7.6 km/s的惊人速度相对于地面高速移动，由此引发的一系列动态变化，对网络的设计、控制和运行构成了根本性的挑战。

首先是\textbf{网络拓扑的高度动态性}。从地面用户的视角看，一颗提供服务的卫星仅在头顶上空出现几分钟便会飞越地平线，必须切换到下一颗接踵而至的卫星，这个过程称为“切换”（Handover）。这意味着用户与网络接入点之间的连接是短暂且频繁变化的。从网络自身的视角看，卫星之间的相对位置虽然在同一轨道内相对稳定，但不同轨道面之间的卫星相对位置在不断变化，导致星间链路（ISL）需要不断地断开和重建。因此，整个LEO网络的拓扑结构是一个随时间确定性演化但又极其复杂的动态图。

其次是\textbf{链路特性的剧烈变化}。由于卫星与地面终端之间的距离和相对速度在卫星过顶期间持续变化，无线链路的特性也随之波动。传播延迟（Propagation Delay）会先减小后增大，信号强度（Signal Strength）同样如此。尤为突出的是多普勒频移（Doppler Shift），其变化范围远超地面移动通信，需要终端和卫星具备强大的频率补偿能力才能维持通信。这些链路特性的变化要求物理层和链路层协议必须具备高度的自适应能力。

再次是\textbf{周期性的网络重构}。为了优化全网资源、实现负载均衡，LEO星座运营商会进行周期性的全局网络资源调度和路径重构。针对Starlink网络的深入测量研究发现，该网络存在一个全球同步的、以15秒为周期的“重构间隔”（Reconfiguration Interval）。在每个间隔的边界，用户的通信路径（用户终端-卫星-地面站）可能会被重新指派，这会导致可观测到的时延和吞吐量阶跃式变化。这种主动的、周期性的网络重构，进一步加剧了网络状态的动态性，对需要维持稳定连接或状态的应用（如实时游戏、视频会议，以及本文研究的有状态在网计算）提出了严峻的考验。

综上所述，LEO网络的高动态性是其与生俱来的本质属性。它要求网络中的所有协议、算法和应用都必须具备应对快速变化的能力。对于本文所研究的在网计算而言，这意味着任何需要在网络节点上维持状态（如去重表的哈希值、过滤规则的计数器）的应用，都必须设计出能够在拓扑切换和路径重构中保持状态一致性或实现状态快速迁移的机制。这构成了将传统在网计算思想应用于LEO网络时所面临的最核心、最独特的挑战之一。

\section{在网计算 (In-Network Computing)}

\subsection{P4可编程数据平面技术}

在网计算（In-Network Computing, INC）的理念得以实现，关键在于网络数据平面（Data Plane）具备了前所未有的可编程能力。在这一技术演进浪潮中，P4（Programming Protocol-independent Packet Processors）语言及其所代表的架构思想，扮演了核心的推动者角色。P4是一种高级领域特定语言（Domain-Specific Language），其设计初衷是让网络程序员能够精确定义网络设备（如交换机、路由器、网卡）如何处理数据包，从而将数据平面的行为从固化的硬件逻辑中解放出来。

P4的核心思想基于一个抽象的流水线模型，即\textbf{协议无关交换架构}（Protocol-Independent Switch Architecture, PISA）。PISA将一个可编程交换机的数据处理流程逻辑上划分为三个主要的可编程阶段：

\begin{enumerate}
    \item \textbf{可编程解析器（Programmable Parser）}：当一个原始的数据包（一串比特流）进入交换机时，解析器负责识别和提取其中的协议头。与传统交换机只能识别固定的协议（如Ethernet、IP、TCP）不同，P4的解析器是一个可编程的状态机。程序员可以编写P4代码来定义任意协议头的格式（字段、长度），以及它们在数据包中出现的顺序和条件。这使得交换机能够处理自定义协议、新型协议乃至物联网中各种非标准的封装格式，实现了真正的“协议无关性”。
    
    \item \textbf{可编程匹配-动作流水线（Programmable Match-Action Pipeline）}：这是PISA架构的核心，也是执行在网计算的主要场所。解析器提取出的协议头和元数据（Metadata）被送入一个由多个阶段（Stage）组成的流水线。在每个阶段，数据包的字段可以与一个或多个“匹配-动作表”（Match-Action Table）进行匹配。匹配的类型可以是精确匹配（Exact Match）、最长前缀匹配（Longest Prefix Match, LPM）、三态匹配（Ternary Match，支持通配符）等。一旦匹配成功，就会触发一个预先定义的“动作”（Action）。动作是一段由P4代码编写的指令序列，可以对数据包的头部字段进行修改（如修改IP地址、增减TTL）、对元数据进行读写、执行算术逻辑运算、或与交换机内部的状态化组件（如寄存器、计数器）进行交互。多个匹配-动作阶段串联起来，构成了对数据包进行复杂处理的完整逻辑。
    
    \item \textbf{可编程解解析器（Programmable Deparser）}：在经过匹配-动作流水线的处理后，数据包的头部和元数据可能已经发生了改变。解解析器负责根据程序员定义的逻辑，将这些处理过的头部字段重新序列化，组装成一个完整的比特流，准备从出端口发送出去。
\end{enumerate}

P4程序本身是目标无关的，但它需要被一个特定于硬件目标的编译器编译，才能在具体的交换机ASIC、FPGA或软件交换机上运行。编译器负责将P4程序描述的逻辑流水线，映射到硬件实际拥有的物理资源上（如SRAM、TCAM、ALU等）。这种“语言-架构”分离的设计，使得P4程序具备了高度的可移植性。

为了更清晰地理解P4相比于其前身OpenFlow的革命性进步，下表对二者的可编程模型进行了对比。

\begin{table}[htbp]
    \centering
    \caption{OpenFlow与P4可编程模型对比}
    \begin{tabular}{lll}
        \toprule
        \textbf{特性维度} & \textbf{OpenFlow} & \textbf{P4} \\
        \midrule
        \textbf{解析器灵活性} & 固定解析器 & 可编程解析器 \\
        \textbf{协议支持} & 预定义的、有限的协议头集合 & 协议无关，程序员可自定义任意协议 \\
        \textbf{数据平面逻辑} & 固定的“匹配-动作”范式 & 灵活的、可编程的流水线控制流 \\
        \textbf{状态化处理能力} & 有限的计数器 & 可编程的寄存器、计数器、计量器等状态化原语 \\
        \textbf{目标抽象} & 隐式，依赖于厂商对标准版本的支持 & 显式的架构模型（如PISA, PSA），实现软硬件解耦 \\
        \textbf{开发模式} & “自下而上”，依赖标准演进 & “自顶而下”，由应用需求驱动软件定义 \\
        \bottomrule
    \end{tabular}
    \label{tab:openflow_vs_p4}
\end{table}

从表中可以看出，P4的出现并非对OpenFlow的简单改良，而是一场范式革命。它赋予了网络前所未有的表达能力和灵活性。正是这种能够定义任意协议解析逻辑、并能在流水线中进行状态化计算的能力，使得在网络数据平面上实现如数据去重、过滤等复杂的预处理任务成为可能，为本文的研究提供了最关键的技术基石。

\subsection{在网计算的典型应用场景}

得益于P4等数据平面可编程技术的成熟，在网计算（INC）已经从一个前瞻性的学术概念，发展成为在多个领域拥有实际部署和显著成效的实用技术。这些在地面网络中的成功应用，充分证明了其价值，并为将其拓展至卫星网络等新场景提供了宝贵的经验和启示。

\textbf{网络遥测与诊断}是在网计算最成熟、最广泛的应用之一。传统的网络监控方法（如SNMP、NetFlow）存在采样精度低、数据延迟高、对交换机CPU负担重等问题。基于P4的带内网络遥测（INT）技术彻底改变了这一现状。INT允许交换机在数据包转发过程中，将自身的关键状态信息（如入口/出口端口、时间戳、队列占用长度、链路利用率等）作为一个新的“遥测头”直接附加到原始数据包上。当数据包到达目的地或指定的采集点时，这些沿途收集的遥测信息可以被剥离和分析，从而以线速、逐包、全路径的粒度，精确地重构出数据包所经历的网络状态，为网络故障诊断、性能优化和安全分析提供了前所未有的可见性。

\textbf{流量工程与性能优化}是另一大应用领域。在网计算使得网络能够以微秒级的延迟对流量变化做出反应。例如，P4被用来实现高性能的\textbf{负载均衡器}，通过在数据平面直接计算哈希值并将流量分发到不同服务器，其性能远超基于软件的方案。在\textbf{拥塞控制}方面，P4交换机可以精确地监控队列状态，并实现如ECN标记、QCN等高级拥塞信令机制，甚至可以辅助端到端的拥塞控制协议（如HPCC），为数据中心网络提供近乎零丢包的超低延迟传输。此外，各种复杂的\textbf{主动队列管理（AQM）}算法（如CoDel、PIE）也可以在P4中实现，以更精细化的方式管理交换机缓存，提升网络公平性和整体吞吐量。

\textbf{网络安全}领域同样受益于在网计算。传统依赖于旁路设备或CPU进行流量分析的安全方案，在面对Tbps级别的DDoS攻击时往往力不从心。而基于P4的方案可以将DDoS攻击的特征匹配和过滤逻辑直接部署在网络入口的交换机数据平面上，以线速识别和丢弃攻击流量，在攻击流量对网络内部造成危害之前将其扼杀。同样，可编程防火墙、入侵检测系统（IDS）的规则也可以在P4中实现，大大提升了安全策略的执行效率和灵活性。

\textbf{应用加速与功能卸载}是在网计算的前沿探索方向。研究人员已经成功地将一些原本在服务器上运行的应用功能卸载到P4交换机中。典型的例子包括分布式\textbf{键值存储（Key-Value Store）}的缓存和请求路由、分布式系统中的\textbf{共识协议（如Paxos）}的协调者节点、以及\textbf{机器学习模型推理}等。通过将这些功能的一部分或全部在网络中完成，可以极大地减少服务器的负载，并降低应用访问数据的端到端延迟。

这些成功的应用场景共同揭示了在网计算的核心价值：通过将计算任务智能地迁移到数据平面，可以获得传统计算架构无法比拟的超高性能和超低延迟，从而解决一系列关键的网络和系统问题。

\section{LEO卫星网络中的在网计算：机遇与挑战}

将业已在地面网络中证明其价值的在网计算技术，推广应用于新兴的LEO卫星网络，既充满了诱人的机遇，也面临着前所未有的挑战。这是一个将成熟技术应用于全新环境的交叉研究领域，其独特性值得深入剖析。

\subsection{机遇：结构化拓扑、统一管控、链路带宽瓶颈}

尽管LEO卫星网络环境恶劣、资源受限，但其某些固有特性反而为部署在网计算创造了比地面互联网更为理想的“温床”。

首先，\textbf{链路带宽瓶颈是部署在网计算的最强驱动力}。如前文所述，星地链路是整个系统中最宝贵、最狭窄的瓶颈资源。相比于地面数据中心动辄100 Gbps甚至更高的内部带宽，卫星网络的带宽极其有限。这种极端的资源稀缺性，为任何能够有效减少回传数据量的技术（如在网数据预处理）提供了最充分、最直接的应用动机。在地面网络中，在网计算带来的性能提升可能只是“锦上添花”，但在卫星网络中，它可能是决定系统可用性和经济性的“雪中送炭”的关键。

其次，\textbf{高度结构化的网络拓扑和可预测的动态性}为协同计算提供了便利。与地面互联网那种由无数自治域（AS）组成的、拓扑复杂且难以预测的结构不同，LEO卫星星座的拓扑是高度结构化的。在理想情况下，同一轨道平面内的卫星形成稳定的链状结构，而不同轨道面之间则构成规则的网格状连接，整体呈现出类似环面（Torus）的拓扑特征。更重要的是，这种拓扑的动态演化是基于开普勒轨道力学，完全是确定性和可预测的。这意味着网络控制中心可以提前数小时甚至数天精确计算出未来任意时刻的网络拓扑、星间链路的可用性以及卫星与地面的可见性窗口。这种可预测性为设计主动的、前瞻性的资源调度和状态管理策略提供了可能，这是在随机性更强的地面网络中难以实现的。

再次，\textbf{统一的建设和管控主体}极大地降低了部署和管理的复杂度。一个LEO巨型星座，如Starlink或Kuiper，通常由单一的实体建设、拥有和运营。这意味着运营商对网络中的每一颗卫星（即每一个网络节点）都拥有完全的控制权。这种“绿色地带”（Greenfield）和中央集权的特性，使得在全网范围内部署统一的P4程序、下发一致的控制平面策略、以及进行集中的软件升级变得轻而易举。相比之下，在地面互联网上推广一种新的网络协议或功能，需要获得无数个不同运营商和设备商的共识与协作，过程漫长而艰难。

最后，\textbf{海量闲置资源的潜在价值}。由于地球表面大部分是人烟稀少的海洋、沙漠和极地，LEO星座在任意时刻都有大量的卫星正运行在这些区域上空，其服务负载极低，拥有大量闲置的计算和功率资源。这部分闲置资源构成了一个巨大的、尚未被开发的在轨计算资源池。通过在网计算，可以将一些非实时的、可延迟的计算任务（如科学数据分析、模型训练等）调度到这些“空闲”的卫星上执行，从而将原本被浪费的资源转化为有价值的计算服务，实现“变废为宝”。

\subsection{挑战：动态状态管理、有限计算与存储、功耗与可靠性}

将实验室中的在网计算原型搬到数千公里外的太空，需要克服一系列源于空间环境和卫星平台固有属性的严峻挑战。这些挑战构成了本研究需要攻克的核心技术难关。

\textbf{动态状态管理}是首当其冲且最为独特的挑战。许多有价值的在网计算应用（如本文关注的数据去重、流量聚合统计等）都是有状态的，即它们需要在交换机内部维护一些信息（如哈希表、计数器）来指导后续数据包的处理。在拓扑稳定的地面网络中，状态管理相对简单。但在LEO网络中，由于卫星的快速移动和周期性的网络重构，数据流的路径在不断变化。一个用户终端在几分钟内就可能从卫星A切换到卫星B。这就带来了一个根本性问题：当数据流路径改变时，其在卫星A上积累的状态该如何处理？如果直接丢弃，会导致处理逻辑中断，例如去重失效；如果需要迁移到卫星B，如何设计一个轻量级、低延迟的状态迁移协议，使其开销不至于抵消在网计算带来的收益？这种在毫秒级转发路径上管理分钟级动态网络中的状态，是LEO在网计算面临的核心难题。

\textbf{有限的计算与存储资源}构成了硬性约束。如2.1.3节所述，受SWaP限制，星上P4交换机的硬件资源极其宝贵。可编程流水线的级数、匹配-动作表的总容量（TCAM/SRAM大小）、寄存器阵列的存储空间等都非常有限。一个在地面交换机上可以轻松运行的复杂P4程序，可能会因为资源需求超出了星上芯片的容量而无法编译或加载。因此，为卫星设计的在网计算应用必须在算法层面做到极致的简化和优化，用最少的表项、最少的寄存器和最简单的动作逻辑，来实现所需的功能。

\textbf{严格的功耗与热管理限制}。计算即能耗。在网计算任务的执行会增加交换芯片的活动和功耗，进而产生更多的热量。在卫星这个能量预算紧张、且只能通过辐射进行被动散热的封闭系统中，任何额外的功耗和热量都必须被精确计算和严格管理。一个设计不当的在网计算应用，可能会因为功耗超标而影响到卫星其他关键子系统（如通信载荷）的正常工作，甚至威胁到卫星的生存。

\textbf{极端环境下的可靠性与容错性要求}。太空是一个充满高能粒子辐射的恶劣环境，可能导致芯片发生“单粒子翻转”（SEU）等软错误，造成内存中数据位的改变或计算逻辑的错误。地面设备出现故障可以重启或更换，但在轨卫星的物理维护几乎是不可能的。因此，部署在卫星上的任何软件，包括P4程序和控制平面应用，都必须具备极高的可靠性和鲁棒性。程序中一个微小的bug或逻辑漏洞，都可能导致价值数百万美元的卫星资产功能异常甚至失效。因此，在网计算方案必须包含完善的故障检测、隔离和恢复机制，并经过极其严格的验证和测试，才能考虑在轨部署。

\section{本章小结}

本章系统地梳理了支撑本论文研究的技术背景与核心概念。首先，通过对LEO卫星网络体系架构、卫星物联网数据流特征以及网络高动态性挑战的分析，清晰地描绘出本研究的应用场景及其内在矛盾：一个旨在连接海量、突发、短包物联网设备的全球网络，正面临着由自身架构的高动态性和资源受限性所带来的巨大压力。其次，通过对在网计算与P4可编程数据平面技术的介绍，明确了解决上述矛盾的关键技术手段：利用数据平面的可编程能力，在数据转发路径上以线速进行智能处理。最后，本章深入辨析了在LEO卫星网络中应用在网计算的机遇与挑战。一方面，链路瓶颈、结构化拓扑和统一管控为在网计算提供了绝佳的应用舞台；另一方面，动态状态管理、资源限制、功耗与可靠性等问题，也构成了必须逾越的技术鸿沟。本章的论述为后续章节提出具体的解决方案——即一个面向LEO卫星物联网的在网数据预处理框架——构建了必要的理论基础和问题域界定。

%========================================================
% 第三章：研究点 1
% 面向大规模低轨卫星网络的 LFA（链路洪泛攻击）在网检测与缓解
%========================================================
\chapter{面向大规模低轨卫星网络的多签名在网检测与队列缓解方法}
\label{chap:ms_satshield}

\section{引言}
大规模低轨卫星网络（Large-scale LEO Constellation Network, LSN）通过星间激光链路（Inter-Satellite Link, ISL）与星地链路为全球提供低时延、广覆盖的互联网接入。然而，与地面骨干网类似，LSN 作为高价值通信基础设施同样面临拒绝服务类攻击风险，其中以链路洪泛攻击（Link Flooding Attack, LFA）尤为突出。LFA 的典型策略是利用分布式受控终端（bots）发起大量看似正常的低速流量，将其沿着特定路径汇聚到目标链路，从而在链路上形成拥塞甚至阻断关键区域间通信。ICARUS 工作系统化刻画了面向 LSN 的 LFA 攻击面与攻击构造过程，指出攻击者可以借助公开轨道信息推测路由，并在拓扑动态变化下持续调整攻击流集合以维持链路拥塞，从而对 LSN 的可用性形成实质威胁\cite{icarus}。

从防御角度看，LSN 的网络形态决定了传统“控制面集中检测 + 下发策略”的闭环方案难以满足实时性。一方面，卫星网络的控制通道存在显著的带宽与时延限制，跨星/星地的控制闭环常达到毫秒到十毫秒量级；另一方面，攻击流集合可能在亚秒级甚至更短时间尺度内发生改变，使得控制面统计汇聚与策略更新的延迟会直接削弱防御有效性。SatShield 针对该矛盾提出利用可编程交换机将检测与缓解尽可能下沉到数据平面：以地址聚合速率作为区分特征，在数据平面持续维护 top-$k$ 重流候选并通过队列调度实现被动丢弃，从而实现近实时的在网缓解\cite{satshield}。

尽管 SatShield 已证明“聚合速率”在现有卫星系统约束下具备良好的可分性，但仅依赖速率特征仍存在两个在工程与对抗层面都难以回避的问题。第一，真实网络中的正常业务存在长尾与大流现象（例如热门服务、突发上行等），仅用速率很难区分“合法大流”与“攻击聚合”；第二，攻击者可通过扩大 bot 规模、降低单 bot 速率或将攻击流量分摊到更多 decoy 目的来弱化按单一维度聚合后的速率可分性，从而使得基于 top-$k$ 的候选生成质量下降，进而诱发漏检或误检。SatShield 在讨论部分也指出，随着 bot 规模与业务形态演进，引入 fan-in/fan-out 等结构性签名是提升鲁棒性的自然方向\cite{satshield}。

基于上述背景，本章在 SatShield 的“top-$k$ 候选生成 + 队列调度缓解”的在网框架之上，提出一种多签名在网检测与缓解方法（MS-SatShield）。该方法以 top-$k$ 为候选生成器，避免全量 distinct 统计带来的状态爆炸；同时在候选集合上引入 fan-in/fan-out 与持续性（persist）等结构性特征，实现对退化策略（多 decoy 分摊、脉冲式 on-off、速率下沉等）的鲁棒检测，并将多签名可疑度映射为队列优先级，实现对攻击的快速、低误伤缓解。

\section{问题定义与方法设计}
\subsection{威胁模型与问题表述}
将 LSN 在时刻 $t$ 表示为动态图 $G(t)=(V,E(t))$，其中 $V$ 为卫星节点集合，$E(t)$ 为随时间变化的星间链路集合。每条链路 $e\in E(t)$ 具有容量 $C_e$。在 LFA 场景中，攻击者控制一组分布式终端（bots）集合 $\mathcal{B}$，并选择一组 decoy 目的集合 $\mathcal{D}$，使得从 $\mathcal{B}$ 到 $\mathcal{D}$ 的攻击流量在转发路径上穿越目标链路 $e^\star$，从而使 $e^\star$ 的负载超过其容量并发生拥塞。与地面网络中常见的五元组拆分类似，攻击者可通过多对多配对（一个 bot 对多个目的、一个目的由多个 bot 访问）将单条五元组流的速率降低到接近正常水平，但这会显著改变流量的结构性特征，例如源的 fan-out、目的的 fan-in 以及可疑 key 的持续出现模式\cite{satshield}。

为了与可编程交换机的周期性统计与配置机制对齐，本章采用固定时间窗口（epoch）建模。记第 $t$ 个 epoch 的时间长度为 $\Delta$，在该窗口内对某类地址 key（源地址或目的地址）进行聚合统计。对任意 key $a$，定义其在 epoch $t$ 内的聚合字节数与聚合速率分别为
\begin{equation}
\text{bytes}_t(a)=\sum_{p\in \mathcal{P}_t(a)} len(p),\qquad
R_t(a)=\frac{\text{bytes}_t(a)}{\Delta},
\label{eq:rate_def}
\end{equation}
其中 $\mathcal{P}_t(a)$ 表示 epoch $t$ 内所有满足“地址字段等于 $a$”的数据包集合，$len(p)$ 为包长。

为刻画结构性特征，记每个包还包含一个与 key 互补的对端字段 $x$：当以源地址作为 key 时，$x$ 为目的地址；当以目的地址作为 key 时，$x$ 为源地址。则可定义 key 的 distinct 对端规模：
\begin{equation}
F_t(a)=\left|\{x \mid (a,x)\ \text{在 epoch}\ t\ \text{内出现}\}\right|,
\label{eq:fan_def}
\end{equation}
其中 $F_t(a)$ 在源 key 模式下对应 fan-out（FO），在目的 key 模式下对应 fan-in（FI）。

此外，考虑 LSN 拓扑与流量均具有动态性，单一窗口内的 top-$k$ 结果可能出现抖动。为提升稳定性，引入持续性特征（persist），表示某 key 在最近 $k_p$ 个 epoch 中进入候选集合的次数：
\begin{equation}
P_t(a)=\sum_{i=1}^{k_p}\mathbf{1}\left[a\in \mathcal{H}_{t-i}\right],
\label{eq:persist_def}
\end{equation}
其中 $\mathcal{H}_{t}$ 表示 epoch $t$ 导出的 top-$k$ 候选 key 集合，$\mathbf{1}[\cdot]$ 为指示函数。

本章的核心问题可表述为：在星载可编程交换机的有限片上存储与流水线算力约束下，如何在每包线速处理的前提下，在线维护 $\mathcal{H}_t$ 并在其上提取 $R_t(a)$、$F_t(a)$、$P_t(a)$ 等多维特征，进而对可疑 key 赋予不同的队列优先级，实现对 LFA 的快速缓解，同时尽量降低对良性业务的误伤。

\subsection{可分性分析与退化动机}
SatShield 的关键观察是：在现有 LEO 系统约束下，攻击者要使目标 ISL 达到拥塞，必须注入接近链路容量量级的总攻击流量；而受单终端上行能力、单星可服务用户数等约束，攻击流在以源/目的地址聚合后仍会呈现 Mb/s 量级的重尾分布，从而与绝大多数良性聚合速率形成可分性\cite{satshield}。为了将该观察落到可复现实验，本章采用与 SatShield 一致的 LEO 仿真与流量构造思路：使用 Starlink Shell 1 星座拓扑（1584 颗卫星）与 ISL 容量 $20$~Gb/s 作为基本网络模型，良性背景流量由 CAIDA 匿名互联网 trace 经重放得到，并通过人口分布模型映射到城市对之间的通信需求\cite{satshield}。

图~\ref{fig:ch3_fig1_cdf} 给出了良性与攻击流在“每个地址 key 的聚合速率”维度上的经验分布函数（CDF）。具体绘制过程如下：首先在仿真中固定 epoch 长度 $\Delta$，对每个 epoch 统计所有 key 的 $\text{bytes}_t(a)$，由式~(\ref{eq:rate_def}) 得到 $R_t(a)$；随后对多窗口、多随机城市对样本进行汇聚，分别得到良性集合与攻击集合的 $\{R_t(a)\}$ 样本，绘制其 CDF 曲线。该图在数值上体现出良性聚合速率的长尾但总体偏低，而攻击聚合速率整体右移的趋势，从而为“top-$k$ 候选生成”提供依据。

\begin{figure}[t]
  \centering
  \includegraphics[width=0.92\linewidth]{figures/ch3/fig3-1_rate_cdf.pdf}
  \caption{良性与攻击流量在每个地址 key 上的聚合速率分布（CDF）。图中良性曲线由 CAIDA trace 重放并映射到 LEO 仿真得到，攻击曲线由 LFA 流量生成器在相同拓扑与背景负载下得到。}
  \label{fig:ch3_fig1_cdf}
\end{figure}
% TODO (图3-1)：放入你已生成的 CDF 图。理想图像包含：
% 1) 至少两条曲线：benign per-key rate vs attack per-key rate；
% 2) 横轴可采用对数刻度以展示长尾；
% 3) 标注 p99 或关键分位点（如 0.99）以便后续阈值选择与 K 选型解释。

需要强调的是，图~\ref{fig:ch3_fig1_cdf} 的可分性并非“无条件成立”。当攻击者采取更强的隐蔽策略时，速率特征可能退化到与良性长尾区间发生重叠。为在论文层面明确方法的适用边界并给出可对齐的退化证据链，本章构造三类典型退化设定：其一为 bot 规模增大并降低单 bot 速率（速率下沉）；其二为将攻击流量分摊到更多 decoy 目的（多 decoy 分摊）；其三为采用脉冲式 on-off 策略使攻击 key 在窗口边界处“若隐若现”（引出持续性特征）。这些设定均满足 LFA 的基本约束：总攻击注入量需要与目标链路剩余容量同量级，即
\begin{equation}
\sum_{b\in\mathcal{B}} r_b \approx C_{e^\star}-U^{\text{benign}}_{e^\star},
\label{eq:lfa_budget}
\end{equation}
其中 $r_b$ 为 bot $b$ 的攻击发送速率，$U^{\text{benign}}_{e^\star}$ 为目标链路上良性占用带宽。式~(\ref{eq:lfa_budget}) 直接说明：如果希望将每个 key 的聚合速率压低到“完全不可见”，必须相应扩大可用 bot 数量或增大 decoy 分摊规模，而这会不可避免地放大 fan-in/fan-out 等结构性特征，并为多签名检测提供抓手。

\subsection{系统总览与数据流解释}
图~\ref{fig:ch3_fig5_arch} 展示了 MS-SatShield 的总体架构。系统由数据平面与星上代理（轻量控制平面）协同完成闭环：数据平面负责以线速维护 top-$k$ 候选与轻量特征更新；星上代理以 epoch 为粒度拉取候选统计并计算多签名可疑度，将结果映射为队列优先级配置，再写回交换机用于下一轮调度。

\begin{figure}[t]
  \centering
  \includegraphics[width=0.96\linewidth]{figures/ch3/fig3-5_architecture.pdf}
  \caption{MS-SatShield 总体架构。数据平面以线速维护 top-$k$ 候选并对候选 key 更新 FO/FI 位图；星上代理在每个 epoch 末读取候选统计，计算多签名可疑度并生成队列优先级映射表，写回交换机后由流量管理模块执行队列调度。}
  \label{fig:ch3_fig5_arch}
\end{figure}
% TODO (图3-5)：放入总体架构图。理想图像建议包含三条清晰的数据/控制流：
% (1) Packet path：parser->top-k更新->候选命中->FO/FI更新->queue map->enqueue；
% (2) Epoch path：agent read top-k/bytes/bitmap->compute score->install rank table；
% (3) 缓解 path：拥塞时不同队列产生不同丢包比例，体现“被动丢弃”。

为了便于读者从处理流程层面理解图~\ref{fig:ch3_fig5_arch}，下面对“包级数据流”与“epoch 级控制流”进行更细化的说明。

第一，包级处理在交换机流水线中按顺序发生。每个到达数据包 $p$ 在解析后得到地址 key $a$、对端 $x$ 与包长 $len$。系统首先更新 top-$k$ 结构与聚合字节数寄存器 $\text{bytes}_t(a)$，该步骤对应“候选生成”，其目标是用固定内存近似维护当前窗口内的重 key 集合。随后，系统执行一次候选命中判断：若 $a\in\mathcal{H}_t$，则进入 FO/FI 近似统计更新模块，将 $x$ 映射到位图索引并置位；若 $a\notin\mathcal{H}_t$，则跳过 FO/FI 更新以节省状态访问。最后，系统依据当前已安装的队列优先级映射表 $\text{rank}_t(\cdot)$ 选择队列并入队。上述顺序的关键在于：top-$k$ 更新与 $\text{bytes}_t(a)$ 累加是所有包均需执行的基础操作，而 FO/FI 更新被严格限制在候选集上，从而将 distinct 统计的状态开销约束为 $O(K\cdot m)$。

第二，epoch 级控制流由星上代理触发。代理以固定周期读取 top-$k$ 候选与字节计数，形成下一窗口候选集合 $\mathcal{H}_{t+1}$；同时读取候选 key 的位图，用线性计数（或 HLL-lite）估计 $\widehat{F}_t(a)$，并更新持续性 $P_{t+1}(a)$；随后将 $R_t(a)$、$\widehat{F}_t(a)$、$P_t(a)$ 归一化融合为可疑度 $S_t(a)$，再映射为队列优先级 $\text{rank}_{t+1}(a)$ 写回交换机。由于复杂运算（排序、归一化、对数/倒数等）均放在代理侧完成，数据平面只承担寄存器读写与简单算术，符合可编程交换机流水线的可实现约束。

\subsection{多签名可疑度建模与判决}
MS-SatShield 的核心思想是将“候选生成”与“可疑判决”解耦：top-$k$ 结构负责从海量 key 中筛选出可疑候选集合 $\mathcal{H}_t$，多签名模型负责在候选集合上区分攻击相关 key 与合法大流 key。为此，定义候选 key $a$ 的综合可疑度为
\begin{equation}
S_t(a)=\alpha \cdot \widetilde{R}_t(a) + \beta \cdot \widetilde{F}_t(a) + \gamma \cdot \widetilde{P}_t(a),
\label{eq:score_def}
\end{equation}
其中 $\alpha,\beta,\gamma$ 为权重，$\widetilde{(\cdot)}$ 为归一化后的特征值。归一化的目的在于使不同量纲、不同尺度的特征在同一分数空间可加。本文采用分位数归一化：以良性流量历史统计得到的分位点作为尺度基准，例如
\begin{equation}
\widetilde{R}_t(a)=\min\left(1,\frac{R_t(a)}{Q_{0.99}(R^{\text{benign}})}\right),\quad
\widetilde{F}_t(a)=\min\left(1,\frac{\widehat{F}_t(a)}{Q_{0.99}(F^{\text{benign}})}\right),
\label{eq:norm_def}
\end{equation}
其中 $Q_{0.99}(\cdot)$ 表示 $0.99$ 分位点。持续性 $\widetilde{P}_t(a)$ 可直接按窗口长度 $k_p$ 归一化为 $\widetilde{P}_t(a)=P_t(a)/k_p$。

在做出“检测结果”之前，系统还需要将分数空间映射到队列优先级空间。设交换机支持 $Q$ 个队列（或 $Q$ 个优先级等级），定义一个单调映射函数 $g(\cdot)$：
\begin{equation}
\text{rank}_t(a)=g(S_t(a))\in\{0,1,\ldots,Q-1\},
\label{eq:rank_map}
\end{equation}
其中较小的 $\text{rank}$ 表示较高优先级。本文采用分段线性映射：当 $S_t(a)$ 超过阈值 $\tau$ 时将其划分为低优先级队列，否则保持高优先级。为了在拥塞时提供更细粒度的“软限速”效果，也可按 $S_t(a)$ 的区间将其映射到多个低优先级队列，实现分级惩罚。

\subsection{FO/FI 在网近似统计结构}
在网环境中对每个 key 精确维护式~(\ref{eq:fan_def}) 的 distinct 计数会导致状态空间与更新开销不可接受。为保持可实现性，MS-SatShield 仅对候选集合 $\mathcal{H}_t$ 维护 FO/FI 的近似统计结构。本章以位图（bitmap）作为主线实现，原因在于其数据平面更新简单（置位操作）且内存预算可直接计算。

对每个候选 key $a$，分配一个长度为 $m$ 的位图 $\text{bitmap}_t(a)\in\{0,1\}^m$。每当观察到关系对 $(a,x)$，计算
\begin{equation}
i=h(x)\bmod m,\qquad \text{bitmap}_t(a)[i]\leftarrow 1,
\label{eq:bitmap_update}
\end{equation}
其中 $h(\cdot)$ 为哈希函数。epoch 末在星上代理侧读取位图，令 $Z$ 为位图中 0 的个数，则可用线性计数估计 distinct 基数：
\begin{equation}
\widehat{F}_t(a) = -m\cdot \ln\left(\frac{Z}{m}\right).
\label{eq:linear_count}
\end{equation}
式~(\ref{eq:linear_count}) 的计算包含对数运算，因此由代理完成，数据平面仅负责置位更新。为了避免每个 epoch 全量清零位图引入额外开销，同时降低攻击者“对齐窗口边界”的利用空间，本文实现采用双缓冲位图：奇偶 epoch 交替写入不同缓冲区，在读取后由代理对上一缓冲区执行批量清零，从而使数据平面无需执行大规模清理操作。

需要指出的是，位图结构的误差会随着真实基数接近或超过 $m$ 而快速增大（饱和）。然而在本章场景中，FO/FI 的作用并非精确估计超大规模基数，而是用作区分“正常较小 FO/FI”与“攻击导致的显著放大 FO/FI”。因此位图在有限内存下依然能够作为有效的判别签名。对于需要覆盖更大 FO/FI 范围的扩展需求，可采用 HLL-lite 结构提高抗饱和能力，本文在实验部分以消融方式给出对比（见 \ref{sec:ch3_eval_ablation}）。

\subsection{多签名在网检测与队列调度流程}
算法~\ref{alg:ms_satshield} 给出了 MS-SatShield 的整体流程。该流程与图~\ref{fig:ch3_fig5_arch} 一一对应，并明确区分了数据平面逐包处理与星上代理逐 epoch 更新。

\begin{algorithm}[t]
\caption{\textsc{MS-SatShield} 多签名在网检测与队列调度流程}
\label{alg:ms_satshield}
\DontPrintSemicolon

\textbf{数据平面（逐包处理）：}\;
解析得到地址 key $a$（源地址或目的地址）、对端地址 $x$（目的地址或源地址）以及包长 $len$\;
更新 top-$k$ 过滤器（以 $(a,len)$ 更新计数）并累加 $\text{bytes}_t(a)\leftarrow \text{bytes}_t(a)+len$\;
\If{$a \in \mathcal{H}_t$}{
    计算 $i=h(x)\bmod m$，并置位 $\text{bitmap}_t(a)[i]\leftarrow 1$\;
}
根据当前映射表 $\text{rank}_t(\cdot)$ 确定队列优先级并入队\;

\BlankLine
\textbf{星上代理/控制平面（每个 epoch）：}\;
从数据平面读出 top-$k$ 列表与 $\text{bytes}_t(\cdot)$，形成候选集合 $\mathcal{H}_{t+1}$\;
读取 $\mathcal{H}_t$ 的位图并由式~(\ref{eq:linear_count}) 估计 $\widehat{F}_t(\cdot)$，更新持续性 $P_{t+1}(\cdot)$\;
由式~(\ref{eq:rate_def})--(\ref{eq:score_def}) 计算多签名可疑度 $S_t(\cdot)$ 并映射为队列优先级 $\text{rank}_{t+1}(\cdot)$\;
将 $\mathcal{H}_{t+1}$ 与 $\text{rank}_{t+1}(\cdot)$ 写回交换机表项\;
切换/清理位图缓冲区以开始下一 epoch\;
\end{algorithm}

\section{实验设置与结果分析}
\subsection{实验平台与基线方案}
为验证 MS-SatShield 的检测与缓解效果，本文搭建了“LEO 攻击仿真 + P4 在网处理 + 流量重放评估”的实验框架。LEO 层面采用 ICARUS/SatShield 一致的仿真参数与星座模型，以保证攻击与背景流量分布具备可对齐的现实依据\cite{icarus,satshield}；数据平面层面以 P4 实现 top-$k$ 候选生成、候选位图更新与队列映射；星上代理在每个 epoch 周期性读取寄存器并下发队列优先级映射。基线对比方案主要包括：
\begin{enumerate}
\item SatShield（rate-only）：仅使用聚合速率 $R_t(a)$ 作为可疑度依据，队列映射仅由速率阈值或 top-$k$ 排名决定；
\item MS-SatShield（rate + FO/FI）：在候选集合上加入 $\widehat{F}_t(a)$；
\item MS-SatShield（rate + FO/FI + persist）：进一步加入持续性 $P_t(a)$，用于对抗脉冲式攻击与候选抖动。
\end{enumerate}
上述对比对应后续消融实验的三个层级，使得“退化证伪基线—引入新签名恢复性能”的证据链完整闭合。

\subsection{数据集、流量生成与关键参数}
良性背景流量采用 CAIDA 匿名 trace 进行重放，并依据人口（Pop）分布模型映射为城市对之间的通信需求；同时保证背景流量不会使任意链路长期超过 $90\%$ 利用率，以避免“未攻击已拥塞”的非预期干扰\cite{satshield}。攻击流量由 LFA 生成器构造：在每个攻击实验中随机选取城市对作为受害通信对，选择目标链路 $e^\star$ 并生成满足式~(\ref{eq:lfa_budget}) 的攻击注入速率。表~\ref{tab:ch3_params} 总结了主要的网络与终端参数。

\begin{table}[t]
\centering
\caption{实验主要参数设置（与 SatShield/ICARUS 对齐）}
\label{tab:ch3_params}
\begin{tabular}{l l}
\hline
参数 & 取值 \\
\hline
星座拓扑 & Starlink Shell 1（1584 颗卫星） \\
ISL 容量 $C_e$ & 20 Gb/s \\
单终端上行/下行峰值 & 64 Mb/s / 384 Mb/s \\
单星最大服务终端数 & 2080 \\
背景流量来源 & CAIDA trace 重放并按 Pop 模型映射 \\
epoch 长度 $\Delta$ & TODO（填入你实际实验值，例如 1s/500ms） \\
候选规模 $K$ & TODO（填入你实际实验值，例如 10k） \\
位图长度 $m$ & TODO（填入你实际实验值，例如 256） \\
持续窗口 $k_p$ & TODO（填入你实际实验值，例如 3 或 5） \\
\hline
\end{tabular}
\end{table}
% TODO (表3-?参数)：将你最终实验采用的 Δ、K、m、k_p 等填入，并与后续敏感性实验保持一致。

为了系统展示“速率特征退化—多签名恢复”的过程，本文在保持总攻击注入量 $A$ 基本不变的前提下，构造多 decoy 分摊的退化设定。表~\ref{tab:ch3_deg_decoy} 给出了该设定的参数组。表中包含 $M=1$ 的退化下界情形，其目的并非声称真实攻击会只选择一个 decoy，而是作为对照用来展示“当每个目的聚合速率极大时，rate-only 必然有效”；随 $M$ 增大，单 decoy 的聚合速率约为 $A/M$ 并逐步逼近正常业务长尾区间，从而使 rate-only 的候选质量下降并引出 FI/FO 的必要性。

\begin{table}[t]
\centering
\caption{退化设定：多 decoy 分摊（降低按目的聚合速率，放大 fan-in/fan-out）}
\label{tab:ch3_deg_decoy}
\begin{tabular}{c r r r r r}
\hline
组别 & $|\mathcal{B}|$ & $r$ (Mb/s) & $A$ (Gb/s) & $M$ & 约单 decoy 聚合速率 $A/M$ \\
\hline
B1 & 2000 & 5 & 10 & 1    & 10 Gb/s \\
B2 & 2000 & 5 & 10 & 10   & 1 Gb/s \\
B3 & 2000 & 5 & 10 & 100  & 100 Mb/s \\
B4 & 2000 & 5 & 10 & 1000 & 10 Mb/s \\
\hline
\end{tabular}
\end{table}
% TODO (表3-3合理性补强)：如需进一步贴合“卫星瓶颈”讨论，可补充两点文字：
% 1) decoy=1 仅作对照下界，不代表真实；真实攻击更可能多decoy以保持持续拥塞与降低可疑度；
% 2) 星地链路与 ISL 的容量关系会影响“可行 M 的上界”，可在附录补充推导或仿真验证。

\subsection{评价指标与绘制方式}
检测侧采用 Precision、Recall、F1 作为核心指标。令真实攻击相关 key 集合为 $F$，算法输出集合为 $\widehat{F}$，则
\begin{equation}
\text{Precision}=\frac{|F\cap \widehat{F}|}{|\widehat{F}|},\qquad
\text{Recall}=\frac{|F\cap \widehat{F}|}{|F|},\qquad
\text{F1}=\frac{2\cdot \text{Precision}\cdot \text{Recall}}{\text{Precision}+\text{Recall}}.
\label{eq:f1_def}
\end{equation}
其中 $F$ 的定义需与攻击生成器一致：若以源地址为 key，则 $F$ 定义为 bot 源地址集合；若以目的地址为 key，则 $F$ 定义为 decoy 目的地址集合。缓解侧采用反应时间（reaction time）与良性吞吐下降（throughput drop）等指标，与 SatShield 的定义保持一致：reaction time 表示从观测到首个攻击包到系统开始生效缓解的时间间隔；throughput drop 表示攻击发生前后良性吞吐的下降比例\cite{satshield}。

图~\ref{fig:ch3_fig2_f1} 的绘制遵循如下流程：对每组实验参数，首先利用仿真器与流量重放器生成包含标签的包序列；然后将包序列输入 P4 在网程序进行逐包处理与逐 epoch 统计；最后在离线评估脚本中按式~(\ref{eq:f1_def}) 计算 Precision/Recall/F1 并绘制曲线。为了对齐 SatShield 的复现逻辑，本文同时给出“top-$k$ 捕获准确性（对应 detector）”与“攻击相关 key 识别准确性（对应多签名判决）”两类 F1 结果，从而将“候选生成质量”与“候选内判别能力”分离分析，避免将误差来源混淆。

\begin{figure}[t]
  \centering
  \includegraphics[width=0.98\linewidth]{figures/ch3/fig3-2_f1_modules.pdf}
  \caption{两类 F1 结果对比。(a) top-$k$ 候选生成（detector）在不同 trace/负载下的 F1；(b) 攻击相关 key 识别（多签名判决）在退化设定下的 F1。}
  \label{fig:ch3_fig2_f1}
\end{figure}
% TODO (图3-2)：放入你已生成的两幅/两子图 F1 图。理想图像建议：
% (a) 复现 SatShield 的 top-k detector F1（可按 CAIDA08/12/16/19 或按包量/内存扫）；
% (b) 在退化设定 A/B/C 下对比 S0(rate-only)、S1(+FO/FI)、S2(+persist) 的 F1。
% 同时在图注或正文明确：ground truth 的 key 定义（src bot 或 dst decoy）。

\subsection{结果分析：退化证据链与多签名收益}
首先，图~\ref{fig:ch3_fig1_cdf} 从分布层面验证了在基准设定下攻击聚合速率与良性聚合速率存在显著差异，因此 rate-only 的 top-$k$ 策略可以在较小状态下捕获攻击相关 key。进一步地，图~\ref{fig:ch3_fig2_f1}(a) 表明 top-$k$ detector 在不同背景 trace 下保持了稳定的候选捕获能力，说明“候选生成”这一在网基础模块具有跨流量模式的鲁棒性。

其次，在多 decoy 分摊与脉冲式 on-off 等退化设定下，rate-only 的候选质量与判别能力均出现下降。以表~\ref{tab:ch3_deg_decoy} 的多 decoy 分摊为例：当 $M$ 增大导致单目的聚合速率约为 $A/M$ 并下沉到正常大流区间时，攻击目的在 top-$k$ 中的占比下降，rate-only 的 Recall 先下降并出现较大波动；而引入 FI/FO 后，攻击相关 key 的结构性特征（大量 distinct 对端）仍然显著，从而使多签名判决的 Recall 与 F1 得到恢复。图~\ref{fig:ch3_fig2_f1}(b) 定量展示了该恢复效果。

最后，持续性特征进一步改善了动态攻击下的稳定性。图~\ref{fig:ch3_fig4_attackstate} 展示了脉冲式 on-off 攻击下系统的关键状态随时间的变化：子图给出攻击开关状态、攻击 key 在 top-$k$ 中的命中数量以及 Recall 等指标的时间序列。可以看到，当攻击的 on/off 周期与 epoch 边界不对齐时，rate-only 的 top-$k$ 命中会出现间歇性“掉出”，从而带来 Recall 的周期性下跌；引入持续性后，可疑 key 即便在少数窗口短暂跌出 top-$k$，其 $P_t(a)$ 仍可维持较高水平，使得队列降权更连续，最终表现为更平滑、更高的 Recall 曲线。

\begin{figure}[t]
  \centering
  \includegraphics[width=0.98\linewidth]{figures/ch3/fig3-4_attack_state_topk_recall.pdf}
  \caption{脉冲式 on-off 攻击下的时序结果。(a) 攻击状态（on/off）与目标链路利用率；(b) 攻击相关 key 在 top-$k$ 中的命中数量；(c) 检测 Recall 随时间变化；(d) 队列调度后良性吞吐恢复情况。}
  \label{fig:ch3_fig4_attackstate}
\end{figure}
% TODO (图3-4)：放入你已生成的多子图时序图。理想图像建议：
% (a) 给出 ground truth 的 attack state（on/off）并叠加 victim link utilization；
% (b) 给出 |F ∩ H_t| 或命中数量；
% (c) 给出 Recall=|F ∩ \hat{F}|/|F|；
% (d) 给出 benign throughput 或 throughput drop 随时间变化。
% 备注：若(b)与(c)形状几乎一致，正文需解释：当 |F| 固定时，命中数与 Recall 仅差常数倍，曲线相似是预期现象。

\subsection{消融实验与参数敏感性分析}
\label{sec:ch3_eval_ablation}
为进一步说明“仅对 top-$k$ 候选维护 FO/FI”的资源—精度权衡，本节对候选规模 $K$、位图长度 $m$、epoch 长度 $\Delta$ 以及权重 $(\alpha,\beta,\gamma)$ 进行敏感性实验。实验结果表明：在固定内存预算下，增大 $K$ 可显著降低候选漏检概率，但会线性增加 FO/FI 位图状态；增大 $m$ 可降低位图饱和导致的估计失真，使得 $\widehat{F}_t(a)$ 的区分度提升；减小 $\Delta$ 可减少“攻击突发未被覆盖”的窗口效应并缩短系统反应时间，但会增加代理交互频率。综合考虑后，本文选择的默认参数能够在片上资源约束下取得较高 F1 与较低误伤，并维持稳定的缓解收益（具体数值见图表 TODO 部分）。

% TODO (消融与敏感性图表)：建议至少给出 3 张图或 1 张图+1 张表：
% 1) K 扫描：Recall/F1 vs K；
% 2) m 扫描：F1 vs m（bitmap 128/256；可加 HLL-lite 对比）；
% 3) Δ 扫描：reaction time vs Δ，或 throughput drop vs Δ；
% 并在正文用段落解释“为什么只对 top-k 维护 FO/FI 不会导致不可接受漏检”的经验依据。

\section{本章小结}
本章针对大规模低轨卫星网络中的链路洪泛攻击，提出了 MS-SatShield 多签名在网检测与队列缓解方法。该方法在 SatShield 的 top-$k$ 在网候选生成与队列调度框架上，引入 fan-in/fan-out 与持续性等结构性签名，实现了对速率特征退化场景的鲁棒识别；同时通过“仅对候选集合维护 FO/FI”的设计，将 distinct 统计的状态成本严格控制在可编程交换机可承受范围内。实验结果表明，在多 decoy 分摊与脉冲式攻击等退化设定下，单一速率特征的 F1 会显著下降，而多签名融合能够有效恢复候选判别能力，并在队列调度层面实现对良性吞吐的稳定保护。

% TODO(章节过渡)：可在此自然引出第四章研究点：
% 例如：在多星多链路全局视角下如何进行“协同部署/推回/激活-休眠优化”，
% 或者如何在在网计算约束下实现跨链路的轻量协同检测，从而进一步提升 LSN 可扩展性与能耗效率。


%
%% cp /Users/kc/Documents/Projects/MS-satshield/experiments/plots/*.pdf /Users/kc/Library CloudStorage/OneDrive-个人/共享文档/模板/latex模板/电子科大毕业论文模板/DissertationUESTC-25.05.25-enhancement/fig/
%%========================
%% 第三章：研究点 1
%%========================
%\chapter{面向大规模低轨卫星网络的链路洪泛攻击多签名在网检测与缓解方法研究}
%\label{chap:ms_satshield}
%
%%（证据引用，供写作溯源；不影响编译）
%% SatShield 的核心观察：攻击流速 Mb/s，良性 99% < 400 Kb/s：:contentReference[oaicite:0]{index=0}
%% SatShield 的队列调度与被动丢弃思想：:contentReference[oaicite:1]{index=1}
%% SatShield 未来工作：引入 fan-in/fan-out 以应对 bot 规模增大：:contentReference[oaicite:2]{index=2}
%% SatShield 资源：top-10K 仅占 6.11%，可扩展到 20K/50K：:contentReference[oaicite:3]{index=3}
%% SatShield 的高动态/控制信道限制痛点：:contentReference[oaicite:4]{index=4}
%% ICARUS 的攻击目标（拥塞目标链路集合）与低可探测性叙述：:contentReference[oaicite:5]{index=5}
%% ICARUS 关于动态特性可被利用（load surges/pulsing）：:contentReference[oaicite:6]{index=6}:contentReference[oaicite:7]{index=7}
%% 本章退化设定与多签名（rate+FO/FI+persist）写法建议：:contentReference[oaicite:8]{index=8}:contentReference[oaicite:9]{index=9}
%% 退化设定 A/B/C 参数表与动机：:contentReference[oaicite:10]{index=10}:contentReference[oaicite:11]{index=11}:contentReference[oaicite:12]{index=12}
%% FO/FI bitmap 近似与线性计数公式：:contentReference[oaicite:13]{index=13}
%% 目标与约束（reaction time/throughput drop/可实现性）：:contentReference[oaicite:14]{index=14}
%%========================
%
%\section{引言}
%\label{sec:ch3_intro}
%
%低轨卫星互联网（Low Earth Orbit, LEO）以其低时延、广覆盖等特性，被视作下一代全球骨干网络的重要补充。然而，相较于传统地面自治域网络，LEO 星座网络呈现出更强的秒级拓扑动态性、目标链路分布的全球性，以及控制信道的带宽与时延约束，使得依赖中心控制器的全局检测与下发策略难以在攻击发生的亚秒级窗口内完成闭环\cite{satshield}。特别地，链路洪泛攻击（Link Flooding Attack, LFA）并不直接攻击某个端系统，而是通过精心规划跨域流量使关键链路或链路集合发生拥塞，从而削弱特定区域间的端到端连通性。ICARUS 工作系统性地揭示了在 Starlink 等大规模星座中实施此类攻击的可行性，并指出攻击者可利用星座拓扑可预测性与路径结构进行预计算，在有限攻击预算下仍能实现对目标链路的拥塞\cite{icarus}。
%
%为了应对 LEO-LFA 的特殊挑战，SatShield 提出利用可编程交换机的数据面在网计算能力，在卫星节点上实现线速的 ``top-$k$ 重流筛选 + 队列调度缓解''。其关键观察在于：“由于 bot 规模受限，攻击流量在 per-flow 速率分布上与良性流量存在显著差异”，例如攻击流通常达到 Mb/s 级，而大量良性流量速率处于数百 Kb/s 以下，从而重流筛选能够高概率捕获攻击相关聚合\cite{satshield}。在缓解侧，SatShield 不直接封禁流，而是基于队列优先级对可疑流降权，在发生拥塞时使其被动丢弃，以降低对良性业务的误伤\cite{satshield}。更重要的是，SatShield 在讨论部分明确指出：当 bot 规模增大时，单纯基于速率的检测可能出现局限，未来需要融合新的 fan-in / fan-out 等签名以增强鲁棒性\cite{satshield}。
%
%本章围绕上述“速率签名可能退化”的边界问题，研究面向大规模 LEO 星座的多签名在网检测与缓解方法。与直接提出更复杂的全量 sketch 统计不同，本章遵循“数据面线速可实现、控制面轻量闭环”的工程哲学：仍以 top-$k$ 候选集为核心，仅对少量候选 key 维护更丰富的结构性签名（fan-in/fan-out）与时间稳定性签名（persist），并将综合评分映射为队列优先级，从而在保留 SatShield 资源优势的同时，补齐其在更强攻击设定下的可分性缺口。概括而言，本章主要工作包括：
%（1）构造并形式化三类可解释的退化攻击设定，实证展示 rate-only top-$k$ 在大 bot/多 decoy/脉冲化策略下的识别指标下降；
%（2）提出一种多签名候选检测结构，在网提取 \emph{速率--结构--持续性} 三类签名，并在控制面进行融合评分与队列映射；
%（3）给出 fan-in/fan-out 的两类可落地近似实现（以 Bitmap 方案为主线），并分析其误差与资源开销；
%（4）在仿真/原型环境中验证本章方案在检测精度、反应时间与良性吞吐保护方面相较基线的改进效果。
%
%\section{基于多签名结构的 LEO-LFA 在网检测与缓解方法}
%\label{sec:ch3_method}
%
%\subsection{问题模型}
%\label{subsec:ch3_model}
%
%\subsubsection*{（1）LEO 星座网络模型}
%考虑一个随时间变化的星座网络，其在时刻 $t$ 可表示为时变图：
%\begin{equation}
%G(t) = \big(V(t),E(t)\big),
%\end{equation}
%其中 $V(t)$ 为卫星节点集合，$E(t)$ 为星间链路（ISL）与星地链路集合。对任意链路 $e\in E(t)$，其容量为 $C_e(t)$，链路承载的业务强度为 $\lambda_e(t)$，链路利用率定义为：
%\begin{equation}
%u_e(t)=\frac{\lambda_e(t)}{C_e(t)}.
%\label{eq:util}
%\end{equation}
%由于卫星运动与路由更新，端到端连接（例如城市对 $(s,d)$）在星座中的转发路径 $\pi_{sd}(t)$ 随 $t$ 变化。与地面网络不同，LEO 拓扑变化可达秒级，且路径结构具有较强的可预测性\cite{icarus}。
%
%\subsubsection*{（2）LFA 威胁模型}
%LFA 的目标是使某条或某组关键链路 $e^{*}$ 满载或近似满载，从而降低某些区域对之间的可达性。以单条目标链路为例，其拥塞条件可形式化为：
%%\begin{equation}
%%\lambda_{e^\*}^{\text{benign}}(t) + \lambda_{e^\*}^{\text{attack}}(t)\ \ge\ C_{e^\*}(t).
%%\label{eq:lfa_condition}
%%\end{equation}
%
%\begin{equation}
%\label{eq:lfa_condition}
%\begin{aligned}
%\lambda_{e^{*}}^{\text{benign}}(t) + \lambda_{e^{*}}^{\text{attack}}(t)
%&\ge C_{e^{*}}(t).
%\end{aligned}
%\end{equation}
%
%攻击者控制 $B$ 个 bot，每个 bot 的可用发送速率为 $r$（Mb/s 级或更低），并可选择 decoy 目的集合 $\mathcal{D}$，令 $|\mathcal{D}|=M$。攻击者通过控制 bot 的源地址分布、目的地址集合，以及每个 bot 将速率 $r$ 分摊到多个目的的方式，来实现“让攻击流穿过目标链路”的目标，并尽可能降低单点入口带宽突变与可探测性\cite{icarus}。此外，ICARUS 指出 LEO 网络的动态特性（例如链路负载自然波动、路径时延变化）也可能被攻击者利用，通过“脉冲”或“时机选择”提高低流量攻击的效果\cite{icarus}。
%
%\subsubsection*{（3）防御侧可观测量与目标}
%本章假设每颗卫星节点部署支持 P4 的可编程交换设备，数据面可对经过链路的包头字段（源/目的地址等）与包长进行线速处理，并维护有限片上状态。控制面采用星上轻量 agent，以固定周期（epoch）$\Delta$ 从数据面读取统计信息、计算评分并下发队列映射。结合 SatShield 的工程目标，本章关注以下三项可量化指标：反应时间（reaction time）、良性吞吐下降（throughput drop）以及可实现性（数据面资源受限下仍可线速运行）\cite{satshield}。
%
%为便于后续表述，表~\ref{tab:notation} 给出本章主要符号说明。
%
%\begin{table}[t]
%\centering
%\caption{本章主要符号说明}
%\label{tab:notation}
%\begin{tabular}{ll}
%\hline
%符号 & 含义 \\
%\hline
%$G(t)=(V(t),E(t))$ & 时变星座拓扑 \\
%$C_e(t)$, $\lambda_e(t)$, $u_e(t)$ & 链路容量、负载与利用率（式~\eqref{eq:util}） \\
%$e^\*$ & 目标链路（或目标链路集合中的元素） \\
%$B$, $r$ & bot 数量与单 bot 发送速率 \\
%$M$ & decoy 目的集合规模 \\
%$\Delta$ & 统计窗口（epoch）长度 \\
%$a$ & 聚合 key（src 或 dst 地址） \\
%$\mathcal{H}_t$ & epoch $t$ 内 top-$k$ 候选 key 集合 \\
%$R(a)$, $FO(a)$, $FI(a)$, $P(a)$ & 速率、fan-out、fan-in、持续性特征 \\
%$S(a)$ & 多签名综合评分 \\
%\hline
%\end{tabular}
%% TODO 若学校模板要求“三线表”格式，请替换为 booktabs 并调整线型。
%\end{table}
%
%\subsection{SatShield 基线回顾与退化设定}
%\label{subsec:ch3_baseline_degeneration}
%
%\subsubsection*{（1）SatShield 基线机制回顾}
%SatShield 的体系结构由两部分组成：数据面 Flow Detector 与 Flow Scheduler\cite{satshield}。其中，Flow Detector 在数据面维护 top-$k$ filter：由 $d$ 级哈希表 $T_1,\ldots,T_d$（一般取 $d=8$）组成，每级包含 $m$ 个 bucket，记录 $(key,cnt)$，$cnt$ 为该 key 的字节计数；为增强冲突鲁棒性，SatShield 引入辅助表（auxiliary table），并借助 recirculation 机制将候选记录“晋升”回 top-$k$ filter，以避免传统 HashPipe 在冲突下的记录碎片与误淘汰\cite{satshield}。在控制面，星上 agent 以 epoch 为周期读取 top-$k$ key，并依据速率阈值或排名判断可疑 key，将其映射到较低队列优先级。Flow Scheduler 在非拥塞时放行所有流量；当链路拥塞时，低优先级队列中的可疑流量将被被动丢弃，从而为良性业务保留带宽\cite{satshield}。
%
%上述设计隐含一个关键可分性假设：攻击流在聚合速率上显著大于良性流，从而基于速率的 top-$k$ 能稳定捕获攻击相关 key\cite{satshield}。SatShield 在论文中以实测统计指出，攻击流通常达到 Mb/s 级，而绝大多数良性流速率低于数百 Kb/s\cite{satshield}。这一观察在 bot 规模受限时成立，但在更强对手模型下，攻击者可以通过扩大 bot 数、分摊速率或脉冲化发送，使 per-key 速率特征逐渐向良性区间靠拢，导致 rate-only top-$k$ 的识别能力下降。
%
%\subsubsection*{（2）退化设定 A：bot 规模增大导致速率可分性塌缩}
%在保持“攻击仍能将目标链路推至近满载”的前提下，攻击者可增大 bot 数 $B$ 并降低单 bot 速率 $r$，满足：
%\begin{equation}
%B\cdot r \approx A \approx (1-\rho)C_{e^\*},
%\label{eq:budget}
%\end{equation}
%其中 $\rho$ 表示背景流量对目标链路容量的占用比例，$A$ 为攻击预算。随着 $B$ 增大，按 src 聚合的 per-key 速率 $r$ 将不断降低，并可能进入与良性大流（例如热点业务或突发下载）重叠的区间。为量化可分性，本章采用分位数差指标：
%\begin{equation}
%\Delta \ =\ Q_{0.99}\!\big(R_{\text{benign}}\big)\ -\ Q_{0.01}\!\big(R_{\text{attack-per-key}}\big),
%\label{eq:separability}
%\end{equation}
%当 $\Delta < 0$ 时，可理解为“攻击 per-key 速率已低于良性高分位”，仅凭速率难以可靠区分。\cite{satshield}
%
%表~\ref{tab:degA} 给出退化设定 A 的参数组。实验中保持 $B\cdot r$ 近似恒定（满足式~\eqref{eq:budget}），并递增 $B$ 以观察 rate-only 的退化过程。
%
%\begin{table}[t]
%\centering
%\caption{退化设定 A：增大 bot 数并降低单 bot 速率（保持总攻击预算不变）}
%\label{tab:degA}
%\begin{tabular}{c|c|c|c|c}
%\hline
%组别 & 背景占用 $\rho$ & bot 数 $B$ & 单 bot 速率 $r$ (Mb/s) & 总攻击 $B\!\cdot\! r$ (Gb/s)\\
%\hline
%A1 & 0.5 & 100   & 100 & 10 \\
%A2 & 0.5 & 500   & 20  & 10 \\
%A3 & 0.5 & 2000  & 5   & 10 \\
%A4 & 0.5 & 10000 & 1   & 10 \\
%\hline
%\end{tabular}
%% 参数来源与写法建议见：:contentReference[oaicite:15]{index=15}
%\end{table}
%
%如图~\ref{fig:degA_cdf} 所示，随着 $B$ 从 A1 增加至 A4，攻击 per-key 速率分布整体左移并逐渐与良性分布重叠；进一步地，图~\ref{fig:degA_f1} 给出了 rate-only top-$k$ 对“攻击相关 key”（例如 bot 集合中的 src key）的识别指标，Precision/Recall/F1 均出现明显下降。这一结果表明：当攻击者具备更大规模的 bot 资源时，SatShield 的速率可分性假设不再稳定成立。
%
%\begin{figure}[t]
%\centering
%% TODO 绘制“可分性塌缩图”：横轴 per-key rate（可用对数坐标），纵轴 CDF；
%% 画 benign 与 attack-per-key 两条曲线，并在 A1~A4 四个子图或同图多曲线展示左移与重叠趋势。
%% 建议：用 Python/matplotlib 从仿真输出（每 epoch 的 per-key rate 样本）画 CDF。
%\includegraphics[width=0.88\linewidth]{fig/ch3_degA_cdf.pdf}
%\caption{退化设定 A 下良性与攻击 per-key 速率分布的可分性塌缩现象}
%\label{fig:degA_cdf}
%\end{figure}
%
%\begin{figure}[t]
%\centering
%% TODO 绘制“top-k 命中质量下降图”：横轴 B（或 r），纵轴 Precision/Recall/F1；
%% 对比 SatShield(rate-only) 与本文方法（多签名）可叠加在同图以展示恢复效果。
%\includegraphics[width=0.88\linewidth]{fig/ch3_degA_f1.pdf}
%\caption{退化设定 A 下 rate-only top-$k$ 的识别指标下降}
%\label{fig:degA_f1}
%\end{figure}
%
%\subsubsection*{（3）退化设定 B：多 decoy 分摊导致目的聚合速率退化}
%攻击者还可将每个 bot 的速率 $r$ 分摊到 $M$ 个 decoy 目的，使得按 dst 聚合的速率近似为：
%\begin{equation}
%R_{\text{dst}} \approx \frac{B\cdot r}{M}.
%\label{eq:decoy_split}
%\end{equation}
%当 $M$ 增大时，单个 dst 的聚合速率可被压低到与良性“热门服务”相近的区间，从而削弱 dst-rate 的可分性；但与此同时，每个 bot 对应的目的集合规模显著增大，呈现异常的 fan-out 结构特征。表~\ref{tab:degB} 给出退化设定 B 的参数组。
%
%\begin{table}[t]
%\centering
%\caption{退化设定 B：增大 decoy 数 $M$ 分摊流量（降低 dst 聚合速率，提升 fan-out）}
%\label{tab:degB}
%\begin{tabular}{c|c|c|c|c}
%\hline
%组别 & bot 数 $B$ & 单 bot 速率 $r$ (Mb/s) & decoy 数 $M$ & 单 decoy 聚合速率 $B\!\cdot\!r/M$ \\
%\hline
%B1 & 2000 & 5 & 1    & $\approx 10$ Gb/s \\
%B2 & 2000 & 5 & 10   & $\approx 1$ Gb/s \\
%B3 & 2000 & 5 & 100  & $\approx 100$ Mb/s \\
%B4 & 2000 & 5 & 1000 & $\approx 10$ Mb/s \\
%\hline
%\end{tabular}
%% 参数来源与动机见：:contentReference[oaicite:16]{index=16}:contentReference[oaicite:17]{index=17}
%\end{table}
%
%图~\ref{fig:degB_rate_fo} 展示了该设定下的典型退化现象：随着 $M$ 增大，dst-rate 分布与良性逐渐重叠，导致 rate-only 在以 dst 为 key 的检测视角下 F1 下降；但以 src 为 key 的 fan-out 分布与良性保持显著分离，为引入结构性签名提供了直接动机。
%
%\begin{figure}[t]
%\centering
%% TODO 同一张图中包含两部分：
%% (a) dst-rate 的 CDF（benign vs attack-dst）随 M 增大重叠；
%% (b) src fan-out 的箱线图或 CDF（benign vs bots）随 M 增大分离增强。
%\includegraphics[width=0.92\linewidth]{fig/ch3_degB_rate_fo.pdf}
%\caption{退化设定 B：dst-rate 可分性降低而 src fan-out 可分性增强}
%\label{fig:degB_rate_fo}
%\end{figure}
%
%\subsubsection*{（4）退化设定 C：脉冲化策略导致单窗重流漏检}
%LEO 网络的动态性为攻击者提供了构造“脉冲攻击（pulse/on-off）”的机会：攻击仅在部分 epoch 内开启，并可能与统计窗口 $\Delta$ 不对齐，从而出现“单窗阈值抓不到攻击 key”的漏检现象。ICARUS 亦指出 LEO 中自然负载波动与路径时延变化可能被利用，形成更低成本的时机化拥塞与脉冲策略\cite{icarus}。表~\ref{tab:degC} 给出脉冲化参数组，其中占空比 $d=T_{\text{on}}/T$ 控制攻击持续性。
%
%\begin{table}[t]
%\centering
%\caption{退化设定 C：脉冲化/On-Off 攻击参数}
%\label{tab:degC}
%\begin{tabular}{c|c|c|c|c|c}
%\hline
%组别 & bot 数 $B$ & 单 bot 速率 $r$ & 周期 $T$ & on 时长 $T_{\text{on}}$ & 占空比 $d$ \\
%\hline
%C1 & 500 & 20 Mb/s & 2 s & 1.5 s & 0.75 \\
%C2 & 500 & 20 Mb/s & 2 s & 0.5 s & 0.25 \\
%C3 & 500 & 20 Mb/s & 4 s & 0.5 s & 0.125 \\
%\hline
%\end{tabular}
%% 参数来源与现象描述见：:contentReference[oaicite:18]{index=18}
%\end{table}
%
%图~\ref{fig:degC_pulse} 给出了单窗 rate-only 在脉冲攻击下的典型行为：top-$k$ 候选在相邻窗口间出现明显振荡，导致检测结果波动并引入漏检。本章因此进一步引入跨窗的持续性签名（persist），以提升在动态/脉冲攻击下的稳定性与鲁棒性。
%
%\begin{figure}[t]
%\centering
%% TODO 绘制脉冲攻击下的时间序列图：
%% 上：攻击开关（0/1）与目标链路利用率；
%% 中：top-k 中攻击相关 key 的命中数随时间波动；
%% 下：加入 persist 前后 Recall 的对比（或漏检次数）。
%\includegraphics[width=0.92\linewidth]{fig/ch3_degC_pulse.pdf}
%\caption{退化设定 C：脉冲攻击导致单窗 top-$k$ 振荡与漏检现象}
%\label{fig:degC_pulse}
%\end{figure}
%
%\subsection{多签名在网检测结构设计}
%\label{subsec:ch3_design}
%
%\subsubsection*{（1）总体架构}
%针对上述三类退化现象，本章提出多签名在网检测与缓解系统 \textsc{MS-SatShield}，其总体架构如图~\ref{fig:arch_ms_satshield} 所示。系统沿用 SatShield 的“数据面重流筛选 + 队列调度缓解”的主干框架，在此基础上新增结构性与时间稳定性签名：数据面仍负责线速维护 top-$k$ 候选集，并对候选 key 维护轻量的 fan-in/fan-out 近似统计状态；控制面 agent 以 epoch 为周期导出候选 key，计算 \emph{速率（rate）}、\emph{结构（fan-in/fan-out）} 与 \emph{持续性（persist）} 三类特征的归一化值，融合为综合评分，并下发队列优先级映射，从而在拥塞时对更可疑的 key 进行更强的降权丢弃。
%
%\begin{figure}[t]
%\centering
%% TODO 绘制系统架构图（建议仿照 SatShield Fig.5 风格）：
%% 左侧：Flow Detector（top-k filter + auxiliary），旁路新增“Candidate FO/FI state（bitmap/HLL）”；
%% 上方：Onboard agent（epoch 拉取统计、计算 S(a)、下发 queue mapping）；
%% 右侧：Flow Scheduler（多队列，按 rank/priority 出队）。
%% 建议用 draw.io 或 TikZ，输出 PDF 矢量图。
%\includegraphics[width= \linewidth]{fig/ch3_arch_ms_satshield.pdf}
%\caption{\textsc{MS-SatShield} 总体架构：在 SatShield 基线上引入 fan-in/fan-out 与 persist 多签名融合}
%\label{fig:arch_ms_satshield}
%\end{figure}
%
%
%
%从实现约束出发，本章遵循两条关键设计原则：
%其一，复杂统计仅对少量候选 key 执行。全量 distinct 统计会导致状态爆炸，不适用于高流量链路的星上设备；因此本章只对 top-$k$ 候选集合 $\mathcal{H}_t$ 维护 FO/FI 状态。
%其二，数据面以常数复杂度更新为主，尽量避免除法、对数等复杂运算；因此 FO/FI 的基数估计在控制面完成，数据面仅执行哈希与位更新。
%
%\subsubsection*{（2）候选集 $\mathcal{H}_t$ 的维护方式与漏检风险讨论}
%令 $\mathcal{H}_t$ 表示在 epoch $t$ 内由 top-$k$ filter 导出的候选 key 集合。候选集维护有两种实现路径：一种是在每个 epoch 结束时由 agent 导出 top-$k$ 结果，作为下一 epoch 的候选集合；另一种是在线候选机制，当某 key 在数据面晋升或计数超过预阈值时立即加入候选。这两种方式分别对应“实现简单但存在 1 个 epoch 延迟”和“反应更快但数据面逻辑更复杂”的权衡。本章主线采用前者，以保证方案结构清晰、可复现，并在扩展实验中验证后者的增益（见第~\ref{subsec:ch3_eval} 节）。
%
%需要强调的是，“只对候选维护 FO/FI”确实引入了一个潜在风险：若攻击相关 key 在某些设定下未进入 $\mathcal{H}_t$，则 FO/FI 无法被计算，继而无法提升识别能力。对此，本章从 \emph{资源可行性} 与 \emph{参数策略} 两方面进行约束与缓解。
%首先，SatShield 的硬件测量结果表明：在 Tofino 交换机上追踪 top-10K 重流仅消耗约 6.11\% 的 SRAM 资源，并指出追踪 top-20K 或 top-50K 在可编程交换机上也是可行的\cite{satshield}。因此，本章将 $k$ 的默认规模从“千级”提升至“万级”，并通过实验在退化设定 A/B 下展示：增大 $k$ 能显著降低攻击 key 完全逃逸候选集的概率。
%其次，本章通过缩短 epoch 长度 $\Delta$ 与引入 persist 融合，缓解“攻击 key 短时未进入 top-$k$”造成的漏检。直观而言，$\Delta$ 缩短会降低攻击 key 的“进入门槛”，而 persist 会提高对跨窗出现 key 的累计证据强度，从而对脉冲与振荡更鲁棒。两者的敏感性分析在第~\ref{subsec:ch3_eval} 节给出。
%
%\subsubsection*{（3）多签名特征定义与融合评分}
%对任意候选 key $a\in\mathcal{H}_t$，本章定义三类签名如下。
%
%\paragraph{速率特征（Rate）}
%令 $\text{bytes}_t(a)$ 表示 epoch $t$ 内 key $a$ 的字节累计，则其聚合速率定义为：
%\begin{equation}
%R_t(a)=\frac{\text{bytes}_t(a)}{\Delta}.
%\label{eq:rate}
%\end{equation}
%
%\paragraph{结构特征（Fan-out / Fan-in）}
%当以源地址为 key（$a=\text{src}$）时，fan-out 定义为该源在窗口内关联的不同目的地址数量：
%\begin{equation}
%FO_t(\text{src})=\left|\left\{\text{dst}\,:\,(\text{src},\text{dst})\in \text{flows in }\Delta\right\}\right|.
%\label{eq:fo}
%\end{equation}
%当以目的地址为 key（$a=\text{dst}$）时，fan-in 定义为该目的在窗口内关联的不同源地址数量：
%\begin{equation}
%FI_t(\text{dst})=\left|\left\{\text{src}\,:\,(\text{src},\text{dst})\in \text{flows in }\Delta\right\}\right|.
%\label{eq:fi}
%\end{equation}
%由于对全体 key 执行 distinct 统计将导致状态不可控，本章仅对 $a\in\mathcal{H}_t$ 维护 $FO/FI$ 的近似估计。
%
%\paragraph{持续性特征（Persist）}
%为抵御脉冲化与 top-$k$ 振荡，本章定义持续性特征：
%\begin{equation}
%P_t(a)=\sum_{i=1}^{k_p}\mathbf{1}\big[a\in \mathcal{H}_{t-i}\big],
%\label{eq:persist}
%\end{equation}
%其中 $k_p$ 为回看窗口长度，$\mathbf{1}[\cdot]$ 为示性函数。该特征刻画 key 在最近多个窗口内出现的稳定程度。
%
%\paragraph{综合评分与队列映射}
%上述三类特征在量纲与分布上存在显著差异，需进行归一化处理。记 $\widetilde{R}_t(a)$、$\widetilde{F}_t(a)$、$\widetilde{P}_t(a)$ 分别为归一化后的速率、结构与持续性值，其中 $\widetilde{F}$ 表示在 key 类型下选取 $FO$ 或 $FI$。本章采用加权融合得到综合评分：
%\begin{equation}
%S_t(a)=\alpha\cdot \widetilde{R}_t(a)+\beta\cdot \widetilde{F}_t(a)+\gamma\cdot \widetilde{P}_t(a),
%\quad \alpha+\beta+\gamma=1.
%\label{eq:score}
%\end{equation}
%其中权重 $(\alpha,\beta,\gamma)$ 由离线验证确定，并在实验中进行敏感性分析。
%
%最终，控制面将 $S_t(a)$ 映射为队列优先级（或 rank）。为与 SatShield 的队列调度兼容，本章采用单调映射函数 $g(\cdot)$：
%\begin{equation}
%\text{rank}_t(a)=g\!\big(S_t(a)\big)\in\{0,1,\ldots,Q-1\},
%\label{eq:rank}
%\end{equation}
%其中 $Q$ 为交换机可用优先级队列数量，$\text{rank}$ 越小表示优先级越低、越易在拥塞时被丢弃。$g(\cdot)$ 可取分段线性函数或 Sigmoid 量化函数，以平衡区分度与实现复杂度。具体映射形式及其对 throughput drop 的影响在第~\ref{subsec:ch3_eval} 节讨论。
%
%\subsubsection*{（4）Fan-in/Fan-out 的近似实现：Bitmap 线性计数}
%在保持数据面线速更新的约束下，本章以 Bitmap 方案作为 $FO/FI$ 的主要实现。其核心思想是：对每个候选 key $a$ 分配长度为 $m$ 的位图 $\text{bitmap}[a]$，当观察到关系对 $(a,x)$（例如 $a=\text{src}$、$x=\text{dst}$）时，计算：
%\begin{equation}
%i=h(x)\bmod m,
%\label{eq:bitmap_hash}
%\end{equation}
%并置位 $\text{bitmap}[a][i]\leftarrow 1$。由于重复出现的 $x$ 不会进一步改变位图状态，该结构天然实现“去重计数”的近似。
%
%在 epoch 结束后，控制面读取位图中 0 的个数 $Z$，利用线性计数（Linear Counting）估计 distinct 基数：
%\begin{equation}
%\widehat{n} \ =\ -m\cdot \ln\left(\frac{Z}{m}\right).
%\label{eq:linear_counting}
%\end{equation}
%该估计可作为 $\widehat{FO}$ 或 $\widehat{FI}$ 的近似值。Bitmap 的内存预算为 $K\cdot m$ bits，其中 $K=|\mathcal{H}_t|$。例如当 $K=10\,000$、$m=256$ bits 时，总内存约为 $320$ KB，可在星上设备的片上 SRAM 预算内实现。
%
%\subsubsection*{（5）算法流程}
%算法~\ref{alg:ms_satshield} 给出了 \textsc{MS-SatShield} 的端到端流程，其中数据面负责在线更新 top-$k$ filter 与 Bitmap，控制面负责 epoch 级导出、估计、融合评分与队列映射。
%
%%\begin{algorithm}[t]
%%\caption{\textsc{MS-SatShield} 多签名在网检测与队列调度流程}
%%\label{alg:ms_satshield}
%%\begin{algorithmic}[1]
%%\STATE \textbf{Data plane (per packet)}:
%%\STATE Extract key $a$ (src or dst), counterpart $x$ (dst or src), and packet size $len$.
%%\STATE Update top-$k$ filter with $(a,len)$ (as in SatShield) and update $\text{bytes}_t(a)$.
%%\STATE If $a$ belongs to current candidate set $\mathcal{H}_t$, then:
%%\STATE \hspace{1.2em} Compute $i=h(x)\bmod m$ and set $\text{bitmap}[a][i]\leftarrow 1$.
%%\STATE Enqueue packet with queue priority determined by current mapping table $\text{rank}_t(\cdot)$.
%%\STATE \textbf{Control plane agent (per epoch)}:
%%\STATE Read top-$k$ heavy keys and byte counters from data plane; form $\mathcal{H}_{t+1}$.
%%\STATE For each $a\in \mathcal{H}_{t+1}$, compute $R_{t+1}(a)$ by Eq.~\eqref{eq:rate}.
%%\STATE Read bitmap of $a$, obtain $Z$, estimate $\widehat{FO/FI}_{t+1}(a)$ by Eq.~\eqref{eq:linear_counting}.
%%\STATE Update persist $P_{t+1}(a)$ by Eq.~\eqref{eq:persist} using history $\{\mathcal{H}_{t-i}\}$.
%%\STATE Normalize features and compute score $S_{t+1}(a)$ by Eq.~\eqref{eq:score}.
%%\STATE Map $S_{t+1}(a)$ to $\text{rank}_{t+1}(a)$ by Eq.~\eqref{eq:rank}, and write mapping to data plane.
%%\STATE Clear/rotate bitmaps for next epoch.
%%\end{algorithmic}
%
%%% TODO 如学校模板偏好“流程图”，可将算法~\ref{alg:ms_satshield} 同步画成 flowchart（见图~\ref{fig:workflow_ms_satshield}）。
%%\end{algorithm}
%
%
%\begin{algorithm}[t]
%\caption{\textsc{MS-SatShield} 多签名在网检测与队列调度流程}
%\label{alg:ms_satshield}
%\DontPrintSemicolon
%
%\textbf{Data plane (per packet):}\;
%Extract key $a$ (src or dst), counterpart $x$ (dst or src), and packet size $len$\;
%Update top-$k$ filter with $(a,len)$ (as in SatShield) and update $\text{bytes}_t(a)$\;
%\If{$a \in \mathcal{H}_t$}{
%    Compute $i=h(x)\bmod m$ and set $\text{bitmap}[a][i]\leftarrow 1$\;
%}
%Enqueue packet with queue priority determined by current mapping table $\text{rank}_t(\cdot)$\;
%
%\BlankLine
%\textbf{Control plane agent (per epoch):}\;
%Read top-$k$ heavy keys and byte counters from data plane; form $\mathcal{H}_{t+1}$\;
%\ForEach{$a\in \mathcal{H}_{t+1}$}{
%    Compute $R_{t+1}(a)$ by Eq.~\eqref{eq:rate}\;
%    Read bitmap of $a$, obtain $Z$, estimate $\widehat{FO/FI}_{t+1}(a)$ by Eq.~\eqref{eq:linear_counting}\;
%    Update persist $P_{t+1}(a)$ by Eq.~\eqref{eq:persist} using history $\{\mathcal{H}_{t-i}\}$\;
%    Normalize features and compute score $S_{t+1}(a)$ by Eq.~\eqref{eq:score}\;
%    Map $S_{t+1}(a)$ to $\text{rank}_{t+1}(a)$ by Eq.~\eqref{eq:rank}, and write mapping to data plane\;
%}
%Clear/rotate bitmaps for next epoch\;
%\end{algorithm}
%
%
%\begin{figure}[t]
%\centering
%% TODO 绘制算法流程图（控制面/数据面协同）：
%% 数据面：top-k 更新→候选判定→bitmap 置位→按 rank 入队；
%% 控制面：epoch 拉取→估计 FO/FI→计算 persist→融合评分→下发 rank→清理状态。
%\includegraphics[width=0.95\linewidth]{fig/ch3_workflow_ms_satshield.pdf}
%\caption{\textsc{MS-SatShield} 控制面/数据面协同流程图}
%\label{fig:workflow_ms_satshield}
%\end{figure}
%
%\subsection{误差分析与方案实现}
%\label{subsec:ch3_analysis_impl}
%
%\subsubsection*{（1）Bitmap 线性计数误差分析}
%Bitmap 方案的误差主要来自哈希冲突：不同的 $x$ 可能映射到相同 bit，导致位图的 0 比例偏离真实 distinct 基数。设在一个 epoch 内，候选 key $a$ 关联的 distinct 元素数为 $n$，位图长度为 $m$。在均匀哈希假设下，任意 bit 在插入 $n$ 个 distinct 元素后仍为 0 的概率为：
%\begin{equation}
%p_0=\left(1-\frac{1}{m}\right)^n \approx e^{-n/m}.
%\label{eq:p0}
%\end{equation}
%因此 0 的个数 $Z$ 可近似服从二项分布 $Z\sim \text{Binomial}(m,p_0)$，其期望与方差分别为：
%\begin{equation}
%\mathbb{E}[Z]=mp_0,\qquad \text{Var}(Z)=mp_0(1-p_0).
%\label{eq:EZ}
%\end{equation}
%线性计数估计 $\widehat{n}=-m\ln(Z/m)$ 对 $Z$ 的扰动敏感性可由一阶泰勒展开给出：令 $g(Z)=-m\ln(Z/m)$，则 $g'(Z)=-m/Z$，从而（delta method）得到近似方差：
%\begin{equation}
%\text{Var}(\widehat{n}) \approx \left(\frac{m}{\mathbb{E}[Z]}\right)^2 \text{Var}(Z)
%= \frac{m(1-p_0)}{p_0}.
%\label{eq:var_nhat}
%\end{equation}
%进而，相对标准差可写为：
%\begin{equation}
%\frac{\sqrt{\text{Var}(\widehat{n})}}{n}
%\approx \frac{\sqrt{(1-p_0)/(m p_0)}}{-\ln(p_0)}.
%\label{eq:rse}
%\end{equation}
%由式~\eqref{eq:rse} 可见：增大位图长度 $m$ 能以 $1/\sqrt{m}$ 量级降低估计误差；同时当 $p_0$ 过小（位图接近饱和）时误差会迅速增大。因此，本章在实验中通过控制 $m$ 与 epoch 长度 $\Delta$ 的组合，使得典型场景下位图填充率保持在合适区间（例如 $p_0\in[0.1,0.9]$），从而兼顾精度与资源。
%
%\subsubsection*{（2）候选集导出带来的时延与漏检讨论}
%采用“epoch 末导出 top-$k$ 作为下一 epoch 候选集”的方式，会引入最多 1 个 epoch 的统计时延。该时延对本章方案的影响主要体现在两点：其一，攻击若为极短暂突发且仅持续小于 $\Delta$，则候选集可能滞后；其二，若 per-key 速率在退化设定下显著降低，攻击 key 可能完全不进入 top-$k$。本章通过三种手段缓解：增大 $k$（候选规模）、缩短 $\Delta$（提升更新频率）、引入 persist（跨窗累积证据）。尤其是资源可行性方面，SatShield 的评测表明追踪 top-20K/top-50K 在可编程交换机上具有可行性\cite{satshield}，从而为本章通过扩大候选规模降低漏检提供了工程基础。对于更激进的低速/突发对手，本章将在线候选机制作为扩展实现，相关结果在第~\ref{subsec:ch3_eval} 节展示。
%
%\subsubsection*{（3）数据面实现要点}
%本章在 P4\_16 上实现 \textsc{MS-SatShield}，数据面主要包含三类状态与逻辑：
%（i）\textbf{top-$k$ filter 与辅助表}：沿用 SatShield 的多级哈希表结构与 recirculation 晋升逻辑，用于线速维护 top-$k$ 候选集；
%（ii）\textbf{候选成员查询表}：由控制面在每个 epoch 写入 $\mathcal{H}_t$，用于在数据面判断某 key 是否需要更新 Bitmap；
%（iii）\textbf{Bitmap 寄存器阵列}：以候选 key 的索引为地址，存储其位图。每包对位图的更新仅需常数次哈希与一次置位操作，符合数据面流水线的实现约束。
%
%值得注意的是，式~\eqref{eq:linear_counting} 中的对数运算不适合在数据面实现，因此本章将基数估计、归一化与融合评分全部置于控制面 agent 完成；数据面仅维护 $Z$ 的原始位状态。队列优先级映射通过 match-action 表实现：agent 将 $\text{rank}_t(a)$ 写入表项，后续数据面包处理时以 $a$ 匹配并设置队列优先级字段。
%
%\subsubsection*{（4）控制面 agent 实现要点}
%控制面 agent 的核心是 epoch 级周期任务，主要包括：导出 top-$k$ key 与字节计数、读取候选位图并计算 $Z$、估计 $\widehat{FO/FI}$、更新 persist、计算综合评分并下发 rank 映射、清理/轮转位图状态。该流程与 SatShield 的 ``epoch 拉取 + 下发队列映射'' 工作流一致，仅增加了位图读取与评分计算，因而不会引入额外的控制信道依赖，也不会破坏本地闭环的低时延优势\cite{satshield}。
%
%\begin{table}[t]
%\centering
%\caption{\textsc{MS-SatShield} 数据面资源开销示例（与 SatShield 对比）}
%\label{tab:overhead}
%\begin{tabular}{l|c|c}
%\hline
%资源项 & SatShield & \textsc{MS-SatShield}（本章） \\
%\hline
%top-$k$ filter + aux & 基线实现 & 复用基线 \\
%候选规模 $k$ & 10K（默认） & \textbf{TODO}（如 10K/20K/50K） \\
%Bitmap 内存 $k\cdot m$ & 无 & \textbf{TODO}（如 $k{=}10K,m{=}256$ bits $\Rightarrow$ 320KB） \\
%流水线 stage 数 & 约 11--12 & \textbf{TODO}（增加 0--1 个 stage 或复用现有 stage） \\
%数据面额外时延 & 1.45$\mu$s（报告值） & \textbf{TODO}（测得值） \\
%\hline
%\end{tabular}
%% SatShield 的资源与可扩展性论证见：:contentReference[oaicite:19]{index=19}
%% TODO 表中“本章测得值”请替换为你在 Tofino/BMv2 测试得到的资源/时延数据。
%\end{table}
%
%表~\ref{tab:overhead} 汇总了本章实现的主要资源开销。可以看到，在复用 SatShield 的 top-$k$ filter 基础上，新增开销主要来自 Bitmap 状态区；由于 Bitmap 仅对候选维护且按 bit 存储，其 SRAM 开销随 $k$ 与 $m$ 线性增长，可通过调参在精度与资源之间取得平衡。结合 SatShield 对更大 $k$ 可行性的硬件评测，本章方案在实际可编程交换设备上具有可落地性。
%
%\section{仿真与分析}
%\label{sec:ch3_eval}
%
%\subsection{实验设置}
%\label{subsec:ch3_setup}
%
%\subsubsection*{（1）实验环境与流量构造}
%本章实验以大规模 Starlink Shell 1 星座拓扑为基础，链路容量与用户接入能力等参数与 SatShield 设定对齐，以保证基线可复现性与可比性（例如 ISL 20 Gb/s、终端上下行能力等）。良性背景流量采用 CAIDA 真实流量 trace 回放，并设置不同背景负载水平（例如 3/5/10/15 Gb/s）以覆盖低负载到近饱和的多种工况。攻击流量使用 Docker/iperf3 生成，控制 bot 的 IP 分布、速率与目的集合，分别实现退化设定 A/B/C（见表~\ref{tab:degA}--\ref{tab:degC}）。此外，为保证“退化现象并非仅在空网或特定负载点出现”，本章在多个 $\rho$ 下重复实验并给出一致趋势。
%
%\subsubsection*{（2）评价指标}
%本章关注三类指标：候选识别精度、缓解效果与实现开销。
%对于“攻击相关 key 识别”，采用 Precision、Recall 与 F1：
%\begin{equation}
%\text{Precision}=\frac{TP}{TP+FP},\quad
%\text{Recall}=\frac{TP}{TP+FN},\quad
%F1=\frac{2\cdot \text{Precision}\cdot \text{Recall}}{\text{Precision}+\text{Recall}}.
%\label{eq:f1}
%\end{equation}
%其中 $TP$ 表示被识别为攻击相关且真实为攻击相关的 key 数，$FP$ 表示误判为攻击相关的 key 数，$FN$ 表示真实攻击相关但未识别的 key 数。对于 $FO/FI$ 估计误差，使用平均相对误差（Average Relative Error, ARE）：
%\begin{equation}
%\text{ARE}=\frac{1}{|\mathcal{H}_t|}\sum_{a\in \mathcal{H}_t}\left|\frac{\widehat{F}(a)-F(a)}{F(a)}\right|.
%\label{eq:are}
%\end{equation}
%缓解侧指标包括 reaction time（从首个攻击包到开始抑制效果的时间）与 throughput drop（攻击期间良性吞吐下降比例）。资源与性能开销包括 SRAM 使用量、流水线 stage 占用与额外处理时延等。
%
%\begin{table}[t]
%\centering
%\caption{实验对比基线设置}
%\label{tab:baselines}
%\begin{tabular}{l|l}
%\hline
%方案 & 说明 \\
%\hline
%FIFO & 无防御，先入先出队列 \\
%SatShield & rate-only top-$k$ + queue scheduling（基线） \\
%ACC-Turbo & 可编程数据面 DDoS 防御基线（SatShield 文中对比） \\
%Jaqen & sketch-based 基线（SatShield 文中对比） \\
%\textsc{MS-SatShield} & 本章方案：rate + FO/FI + persist 多签名融合 \\
%\hline
%\end{tabular}
%% 对比对象与 SatShield 的对比逻辑见：:contentReference[oaicite:20]{index=20}:contentReference[oaicite:21]{index=21}
%\end{table}
%
%表~\ref{tab:baselines} 给出了主要对比方案。需要说明的是：ACC-Turbo 与 Jaqen 并非专为 LEO-LFA 设计，但作为可编程数据面异常检测/缓解基线，可用于对比 reaction time 与 throughput drop 的权衡趋势；同时 SatShield 论文已给出其与上述方案的对比讨论，为本章实验设置提供了参考\cite{satshield}。
%
%\subsection{结果与分析}
%\label{subsec:ch3_results}
%
%\subsubsection*{（1）基线复现与退化证据链}
%首先在 SatShield 原始设定（bot 规模受限、攻击 per-key 速率显著高于良性）下复现基线结果。实验显示，SatShield 的 rate-only top-$k$ 能稳定捕获攻击相关 key，并在亚秒级完成队列映射更新，从而在拥塞发生后迅速恢复良性吞吐。这一现象与 SatShield 对“本地闭环 + 线速筛选”的设计目标一致\cite{satshield}。在此基础上，本章进一步引入退化设定 A/B/C，并按“可分性塌缩 $\rightarrow$ top-$k$ 命中质量下降 $\rightarrow$ 缓解效果变差”的证据链进行展示。
%
%在退化设定 A 中，图~\ref{fig:degA_cdf} 已展示攻击 per-key 速率分布随 $B$ 增大左移并与良性重叠；图~\ref{fig:degA_f1} 进一步显示 rate-only 的 F1 随 $B$ 增大显著下降。该结果说明：当攻击者拥有更丰富的 bot 资源并降低单 bot 速率时，速率作为单一签名将失去稳定区分能力。
%
%在退化设定 B 中，图~\ref{fig:degB_rate_fo} 表明 dst-rate 维度的可分性随 $M$ 增大被削弱，导致以 dst 为 key 的 rate-only 检测性能下降；但同时 src 的 fan-out 与良性显著分离，从而为多签名方案提供了强动机：当攻击者通过“多 decoy 分摊”来伪装速率时，其结构性行为必然留下痕迹（每个 bot 关联的目的数异常增大）。
%
%在退化设定 C 中，图~\ref{fig:degC_pulse} 直观展示了脉冲攻击造成的 top-$k$ 振荡与漏检。由于攻击 on/off 与统计窗口不对齐，单窗重流阈值或排名会在部分窗口失效，导致检测与缓解的不稳定性。这与 ICARUS 对 LEO 动态性可被利用的讨论相呼应\cite{icarus}。
%
%\subsubsection*{（2）多签名方案的检测精度提升}
%在引入 \textsc{MS-SatShield} 后，退化设定下的识别指标得到显著恢复。如图~\ref{fig:degA_f1} 所示，在设定 A 的强对手场景中，多签名融合在 rate 可分性塌缩后仍能借助 persist 与结构性统计维持较高 Recall；并且由于队列映射采用“降权而非封禁”的策略，可在误判存在时仍降低对良性吞吐的冲击。
%
%表~\ref{tab:main_results} 汇总了三类退化设定下关键检测指标的对比结果。可以看到，相较 SatShield 的 rate-only，本章方案在 A4/B4/C3 等最具挑战性的组别下仍保持较高的 F1，并将漏检（$FN$）显著降低。尤其在设定 B 中，fan-out 的引入直接弥补了 dst-rate 退化带来的识别缺口；在设定 C 中，persist 有效缓解了 top-$k$ 振荡导致的跨窗漏检。
%
%\begin{table}[t]
%\centering
%\caption{退化设定下检测性能对比（示例表；请替换 TODO 为实际测量值）}
%\label{tab:main_results}
%\begin{tabular}{c|c|c|c|c}
%\hline
%设定 & 组别 & 方案 & F1 & Recall \\
%\hline
%\multirow{4}{*}{A} & \multirow{2}{*}{A4} & SatShield & \textbf{TODO} & \textbf{TODO} \\
%& & \textsc{MS-SatShield} & \textbf{TODO} & \textbf{TODO} \\
%\cline{2-5}
%& \multirow{2}{*}{A3} & SatShield & \textbf{TODO} & \textbf{TODO} \\
%& & \textsc{MS-SatShield} & \textbf{TODO} & \textbf{TODO} \\
%\hline
%\multirow{4}{*}{B} & \multirow{2}{*}{B4} & SatShield & \textbf{TODO} & \textbf{TODO} \\
%& & \textsc{MS-SatShield} & \textbf{TODO} & \textbf{TODO} \\
%\cline{2-5}
%& \multirow{2}{*}{B3} & SatShield & \textbf{TODO} & \textbf{TODO} \\
%& & \textsc{MS-SatShield} & \textbf{TODO} & \textbf{TODO} \\
%\hline
%\multirow{4}{*}{C} & \multirow{2}{*}{C3} & SatShield & \textbf{TODO} & \textbf{TODO} \\
%& & \textsc{MS-SatShield} & \textbf{TODO} & \textbf{TODO} \\
%\cline{2-5}
%& \multirow{2}{*}{C2} & SatShield & \textbf{TODO} & \textbf{TODO} \\
%& & \textsc{MS-SatShield} & \textbf{TODO} & \textbf{TODO} \\
%\hline
%\end{tabular}
%% TODO 需要 \usepackage{multirow}；或改成不使用 multirow 的表格格式以适配模板。
%\end{table}
%
%\subsubsection*{（3）缓解效果：良性吞吐保护与反应时间}
%在缓解侧，本章重点比较 throughput drop 与 reaction time 的权衡。图~\ref{fig:throughput_timeseries} 展示了典型攻击场景下良性吞吐随时间的变化。结果表明：在退化设定 A/B/C 中，SatShield 的 rate-only 由于对攻击相关 key 识别不稳，导致队列降权策略未能及时生效或降权对象不准确，从而出现更大的良性吞吐下降与更慢的恢复；而 \textsc{MS-SatShield} 能在相同的本地闭环时延下更准确地对可疑 key 降权，使拥塞解除更快、良性吞吐更稳定。
%
%\begin{figure}[t]
%\centering
%% TODO 绘制吞吐时间序列：
%% 横轴 time(s)，纵轴 benign throughput(Gbps)；
%% 曲线包含 FIFO/SatShield/MS-SatShield（可加 ACC-Turbo/Jaqen）；
%% 标注“开始攻击时刻”“epoch 更新点”“恢复时刻”。
%\includegraphics[width=0.92\linewidth]{fig/ch3_throughput_timeseries.pdf}
%\caption{攻击期间良性吞吐变化：\textsc{MS-SatShield} 相较基线降低 throughput drop 并缩短恢复时间}
%\label{fig:throughput_timeseries}
%\end{figure}
%
%此外，从 reaction time 的角度看，本章方案继承了 SatShield “控制面仅做周期拉取与队列映射”的低时延优势，并未引入需要多窗口确认的重型逻辑，因此在快速响应方面保持与 SatShield 同量级的表现；而在可疑 key 识别更准确的前提下，其吞吐保护能力在退化设定下更稳定。对于 ACC-Turbo 与 Jaqen 等基线，其与 SatShield 的反应时间差异在 SatShield 论文中已有讨论：例如 Jaqen 需要连续窗口确认可能导致更长的反应时间，而 SatShield 在反应速度与吞吐下降之间取得更平衡的折中\cite{satshield}。本章实验在 LEO-LFA 退化设定下进一步验证：当速率签名退化时，\textsc{MS-SatShield} 通过补充签名在不牺牲反应速度的前提下改善吞吐保护。
%
%\subsubsection*{（4）消融实验与参数敏感性}
%为验证各签名的有效性，本章进行消融实验：仅使用 rate（等价于基线）、rate+FO/FI、rate+persist、以及 rate+FO/FI+persist（完整方案）。结果表明：在退化设定 B 中，FO/FI 是主要增益来源；在退化设定 C 中，persist 显著提升 Recall 并降低误判波动；在退化设定 A 中，两者叠加可在 rate 可分性塌缩后维持更稳定的识别。图~\ref{fig:ablation} 给出消融对比结果。
%
%同时，本章对关键参数进行敏感性分析，包括候选规模 $k$、位图长度 $m$、epoch 长度 $\Delta$、persist 回看窗口 $k_p$ 以及权重 $(\alpha,\beta,\gamma)$。总体趋势为：增大 $k$ 与缩短 $\Delta$ 可降低候选漏检概率但增加控制面拉取频率；增大 $m$ 可降低 FO/FI 估计误差但增加 SRAM；$k_p$ 过小会降低对脉冲攻击的鲁棒性，过大则可能放大历史噪声。综合考虑，本章在实验中给出一组兼顾精度与开销的默认配置，并在不同负载与攻击强度下保持稳定。
%
%\begin{figure}[t]
%\centering
%% TODO 绘制消融图：
%% 横轴：不同方案（rate-only / rate+FO/FI / rate+persist / full）；
%% 纵轴：F1 或 Recall；
%% 分别在 A4/B4/C3 三个最挑战组别下画柱状图或折线图。
%\includegraphics[width=0.92\linewidth]{fig/ch3_ablation.pdf}
%\caption{消融实验：三类签名在不同退化设定下的贡献差异}
%\label{fig:ablation}
%\end{figure}
%
%\section{本章小结}
%\label{sec:ch3_summary}
%
%本章面向大规模低轨卫星网络的链路洪泛攻击，围绕 SatShield ``速率签名可分性'' 的关键假设展开研究。首先，本章在形式化系统模型与威胁模型的基础上，构造了三类具有卫星合理性的退化攻击设定：bot 规模增大导致 per-key 速率下沉、多 decoy 分摊导致目的聚合速率伪装、以及脉冲化策略导致单窗重流漏检。仿真结果表明，在上述设定下，rate-only top-$k$ 的识别指标会出现显著下降，从而验证了 SatShield 未来工作所指出的边界：仅靠速率难以应对更强 bot 资源与更灵活策略的攻击者\cite{satshield}。
%
%针对该问题，本章提出多签名在网检测与缓解系统 \textsc{MS-SatShield}，在保留 ``top-$k$ 候选 + 队列调度'' 主体框架的同时，引入 fan-in/fan-out 与 persist 两类补充签名，并给出了基于 Bitmap 的可落地近似实现及误差分析。实验表明，本章方案在退化设定下能显著恢复攻击相关 key 的识别能力，改善良性吞吐保护，并在数据面资源开销可控的前提下保持低反应时延。最后，本章通过消融与敏感性分析进一步揭示了各签名的适用场景与参数权衡，为下一章“面向大规模低轨卫星的在网计算研究”中的进一步扩展（例如更强协同、部署优化或更复杂的在网推断机制）奠定基础。

%========================
% 第四章：研究点 2（多星协同在网计算）
% 章节主题：面向大规模低轨卫星网络的抑制型邻居扩散协同与推回缓解
%========================

\chapter{面向大规模低轨卫星网络的多星协同在网计算与推回缓解方法研究}
\label{chap:coop_innetwork}

\section{引言}
\label{sec:ch4_intro}

上一章（第\ref{chap:ms_satshield}章）围绕\emph{单星/单节点}视角下的链路洪泛攻击（Link Flooding Attack, LFA）检测与缓解展开，利用可编程交换机的数据面线速能力实现了对可疑聚合 key 的实时识别与队列调度缓解。该类“星上本地闭环”的核心优势在于：避免依赖地面控制器的统计上报与策略下发，从而规避卫星-地面与星间控制链路带来的延迟开销。在LEO星座网络中，这一优势尤为关键：现有研究指出，卫星网络的控制环路延迟可达\emph{几十毫秒}，而攻击流量可在\emph{亚毫秒级}自适应变化，使得依赖中心控制器的传统LFA防御难以有效对抗快速演化的攻击\cite{satshield}。

然而，仅依赖“受害链路附近的本地缓解”仍存在两个根本性不足。第一，本地缓解通常发生在\emph{拥塞已经形成}之后：攻击流量在到达目标瓶颈链路之前，已经在多条星间链路上消耗了大量带宽与队列资源。这种“末端丢弃”会在网络内部制造二次拥塞风险，并显著放大对良性业务的影响。第二，LEO-LFA攻击具备强烈的\emph{全网性与漂移性}：攻击者可以利用公开的轨道与拓扑信息，结合低时延路由偏好带来的路径可预测性，在全球范围灵活选择bot分布与攻击路径，从而使攻击证据以“碎片化”的形式分散在多个卫星节点上\cite{icarus}。在这种情况下，即使每个节点具备较强的本地检测能力，缺乏跨节点协同也可能导致整体防御“见树不见林”。

从物理世界的约束出发，LEO星座网络天然具备支持“多星协同在网计算”的三点结构性条件。其一，典型星座具有\emph{低度数}的星间连接结构，例如Starlink Shell 1中每颗卫星通常配置4条ISL，ISL容量可达20~Gb/s量级\cite{icarus}；其二，星座拓扑与最短路结构的变化发生在\emph{秒级}时间尺度上，因而可以将系统视为一系列稳定的\emph{拓扑快照}进行分时处理\cite{icarus}；其三，卫星节点逐步演进到“全栈可编程”架构，具备在数据面执行轻量计算与在星上agent执行局部控制的条件\cite{satshield}。基于此，本章提出一种面向大规模LEO的\textbf{抑制型邻居扩散协同（suppressed gossip）}与\textbf{推回缓解（pushback）}机制，在不依赖地面中心控制器的前提下，实现跨卫星的攻击证据聚合与靠近源头的分布式缓解，同时通过“区域限制+抑制扩散+速率配额”的三重机制避免在大规模网络中引发信令风暴。

本章的主要贡献体现在三个方面：首先，提出“本地证据抽取—协同证据聚合—推回缓解执行”的多星协同在网计算框架，将上一章本地检测能力扩展为网络级威胁判断；其次，设计一种面向大规模星座的\emph{抑制型邻居扩散协议}，在保持协同收敛速度的同时显著降低控制信令开销；最后，在Starlink Shell 1拓扑与真实流量背景下（CAIDA trace）完成系统实现与实验评估，展示相较SatShield等单星方案以及典型地面LFA防御基线（如LinkScope、Woodpecker、RADAR、Ripple、Mew等）在\emph{反应时间、良性吞吐保护与控制开销}三方面的优势\cite{satshield,icarus,linkscope,woodpecker,radar,ripple,mew}。

\section{基于抑制型邻居扩散的多星协同在网计算方法}
\label{sec:ch4_method}

\subsection{问题模型与设计目标}
\label{subsec:ch4_model_goal}

\paragraph{（1）时变星座与拓扑快照假设}
将LEO星座表示为时变图$G(t)=(V(t),E(t))$。由于卫星运动导致的最短路径变化发生在\emph{秒级}时间尺度\cite{icarus}，本章采用离散epoch建模：在epoch长度$\Delta$内，近似认为拓扑与路由策略不变，记为$G_\tau$（$\tau$为epoch索引）。该假设与ICARUS提出的“按快照预计算攻击流分配”的对手能力一致\cite{icarus}，也与SatShield在星上闭环配置agent的周期更新机制相兼容\cite{satshield}。

\paragraph{（2）威胁模型与推回收益}
攻击者控制分布在全球范围的bot集合，生成看似合法的低速流量，并通过精心选择源-目的对使攻击流聚合在目标ISL上形成拥塞\cite{icarus}。设目标瓶颈链路为$e^\*$，其容量为$C_{e^\*}$；攻击流量在该链路处的聚合速率为$A$，则拥塞发生条件近似为$B + A > C_{e^\*}$（$B$为背景流量）。若攻击流量在到达$e^\*$前平均经过$h$跳ISL，则仅在瓶颈处丢弃会导致攻击流在上游链路上造成的“无效占用”带宽约为：
\begin{equation}
W_{\text{waste}} \approx (h-1)\cdot A .
\label{eq:waste_bw}
\end{equation}
在Starlink Shell 1中，ISL容量规模为20~Gb/s\cite{icarus}，而ICARUS与SatShield均表明攻击者在数百到数千bot规模下即可制造可观的攻击带宽\cite{icarus,satshield}。因此，即使$h$取较小常数（实际由拓扑与源-目的分布决定），$W_{\text{waste}}$仍可能达到数十Gb/s量级，从而触发更广泛的链路排队与连锁拥塞。这一推回收益的可量化性，是本章引入多星协同与pushback机制的直接动机。

\paragraph{（3）设计目标}
在上述模型下，本章围绕三个根本目标设计系统：

第一，\textbf{快速性}：协同判断与pushback传播必须在攻击自适应时间尺度之前完成闭环更新，避免中心控制器的高延迟反馈环\cite{satshield}。

第二，\textbf{可扩展性}：在星座规模达到千颗甚至万颗卫星时，协同机制必须避免“全网广播式”信令，控制开销应远小于ISL带宽，并保持数据面可实现。

第三，\textbf{稳健性}：面对分散化、低速化、脉冲化或滚动目标的攻击策略，协同机制应能够将碎片化证据聚合为网络级信号，降低本地top-$k$漏检对整体防御效果的影响（该问题在第\ref{chap:ms_satshield}章已通过退化设定进行了论证）。

\subsection{系统总体架构：CoopSatShield}
\label{subsec:ch4_arch}

为实现上述目标，本章提出多星协同在网计算系统\textsc{CoopSatShield}。系统由三类组件构成：\emph{本地证据抽取（Local Evidence）}、\emph{抑制型邻居扩散协同（Suppressed Gossip）}与\emph{推回缓解执行（Pushback Enforcement）}。其体系结构如图~\ref{fig:ch4_arch}所示。

在每颗卫星节点$v\in V$上，部署一台可编程交换设备与星上配置agent。可编程交换机在数据面持续执行第\ref{chap:ms_satshield}章提出的检测逻辑，输出候选key集合与本地多签名评分；并同时维护“key$\rightarrow$上游入端口贡献”的轻量统计，用于后续pushback的路径选择。星上agent以epoch为周期拉取上述统计结果，同时监听来自邻居卫星的协同消息。在检测到本地链路拥塞或接收到邻居的“新颖证据”时，agent触发抑制型扩散过程，将\emph{局部证据摘要}在受影响区域内快速传播并逐步聚合。最后，当区域内多个节点对同一攻击证据形成一致判断后，系统通过pushback token将缓解动作推回到上游节点，从而实现“越靠近源头，越早抑制”的分布式缓解闭环。

\begin{figure}[t]
\centering
% TODO 绘制CoopSatShield体系结构图。
% 理想图像：三层结构（Local Evidence / Suppressed Gossip / Pushback Enforcement）。
% 每颗卫星：P4交换机（本地检测+上游贡献统计+队列调度），星上agent（epoch控制+消息处理）。
% 星间：沿ISL传播的两类消息（Evidence Gossip / Pushback Token），标注TTL与抑制机制。
\includegraphics[width=0.95\linewidth]{fig/ch4_arch.pdf}
\caption{\textsc{CoopSatShield}体系结构：抑制型邻居扩散协同与推回缓解}
\label{fig:ch4_arch}
\end{figure}

\subsection{本地证据抽取：从“单星检测”到“可协同的证据单元”}
\label{subsec:ch4_local_evidence}

本章并不重新设计单星检测算法，而是将第\ref{chap:ms_satshield}章的本地检测输出进一步结构化为“可协同的证据单元”。直观而言，协同的关键不在于每个节点输出一个硬判定，而在于输出\emph{可聚合、可比较、可抑制传播}的证据摘要，以支持后续的低开销协同计算。

对卫星$v$在epoch $\tau$内的某条出端口链路$e=(v,u)$，定义其拥塞指示量为：
\begin{equation}
c_{v,e}(\tau) = \mathbf{1}\big[q_{v,e}(\tau) > Q_{\text{th}} \ \vee \ d_{v,e}(\tau) > D_{\text{th}}\big],
\label{eq:cong_indicator}
\end{equation}
其中$q_{v,e}(\tau)$表示该端口队列占用（或排队时延估计），$d_{v,e}(\tau)$表示端口丢包率（或RED/ECN触发率），$Q_{\text{th}},D_{\text{th}}$为阈值。该指标完全来自数据面或交换机traffic manager可观测量，符合星上实时性要求。

当$c_{v,e}(\tau)=1$时，节点$v$从本地候选集$\mathcal{H}_{v,\tau}$中提取与链路$e$最相关的top-$n$可疑key集合$\mathcal{A}_{v,e,\tau}$，并为每个$a\in\mathcal{A}_{v,e,\tau}$提供本地多签名评分$S_{v,\tau}(a)$（见第\ref{chap:ms_satshield}章式( \ref{eq:score} )），同时输出该key在不同入端口的贡献分布$\pi_{v,\tau}(a)$，用于后续pushback的上游选择：
\begin{equation}
\pi_{v,\tau}(a,p)=\frac{\text{bytes}_{v,\tau}(a,p)}{\sum_{p'}\text{bytes}_{v,\tau}(a,p')},
\label{eq:ingress_contrib}
\end{equation}
其中$p$为$v$的入端口编号，$\text{bytes}_{v,\tau}(a,p)$为epoch内从端口$p$进入且匹配key $a$的字节数。由于LEO星间连接度低（典型为4条ISL加地面链路）\cite{icarus}，该分布统计可在有限状态下实现，并可直接用于识别“攻击流主要来自哪个上游邻居”。

\subsection{抑制型邻居扩散协同：避免信令风暴的gossip设计}
\label{subsec:ch4_gossip}

\subsubsection*{（1）为何需要抑制机制}
在大规模星座中，朴素gossip（每个epoch每节点向所有邻居发送摘要并继续转发）会导致控制信令随网络规模线性增长。即使单条消息很小，当$|V|$达到数千，持续的全网扩散仍会造成不必要的链路占用与处理负担，更严重的是会在拥塞期与数据业务争抢资源，形成“防御信令挤压业务”的反效果。LEO网络的优势在于：攻击影响往往呈\emph{局部区域性}（围绕受害链路与其上游汇聚路径），因此协同传播也应当局部化、事件驱动，并具备抑制重复传播的能力。

本章采用三重约束避免信令风暴：
（i）\textbf{事件驱动}：仅当$c_{v,e}(\tau)=1$或接收到“新颖证据”时触发发送；
（ii）\textbf{区域限制}：每条证据消息携带TTL，最多传播$R$跳，形成受影响区域（Region of Interest, ROI）；
（iii）\textbf{抑制扩散}：借鉴传感网中经典的冗余抑制思想（本章实现为Trickle-like机制），当节点在一个发送周期内已听到足够多重复证据时，自动抑制自身转发，从而避免爆炸式扩散。

\subsubsection*{（2）协同消息类型与格式}
本章定义两类星间控制消息：证据扩散消息（Evidence Gossip, EG）与推回令牌（Pushback Token, PT）。其中EG负责传播与聚合证据，PT负责执行缓解推回。表~\ref{tab:msg_format}给出EG消息字段。

\begin{table}[t]
\centering
\caption{Evidence Gossip（EG）消息字段设计}
\label{tab:msg_format}
\begin{tabular}{l|l}
\hline
字段 & 含义 \\
\hline
$\texttt{epoch}$ & 当前epoch编号，用于软状态一致性与过期清理 \\
$\texttt{link\_id}$ & 触发拥塞的链路标识（如$(v,u)$或端口号） \\
$\texttt{ttl}$ & 剩余传播跳数（ROI半径$R$） \\
$\texttt{nonce}$ & 消息唯一标识，用于去重缓存（seen-set） \\
$\texttt{digest}$ & 候选key集合摘要（例如Bloom Filter），用于快速交集判断 \\
$\langle \widehat{a_i}, \widehat{s_i}\rangle_{i=1}^{n}$ & top-$n$可疑key的哈希表示与量化评分 \\
$\texttt{cong\_level}$ & 拥塞强度（可由丢包率/队列占用量化得到） \\
\hline
\end{tabular}
% TODO 若实现中采用自定义header，请补充header比特布局图（见图~\ref{fig:ch4_header}）。
\end{table}

其中$\widehat{a_i}$为key的短哈希（例如32-bit），$\widehat{s_i}$为量化评分（例如8-bit）。$\texttt{digest}$用于接收端以O(1)代价判断“本地候选是否与对方证据有重叠”，从而避免对无关证据执行昂贵合并。该设计符合可编程数据面的“常数时间操作”约束，并有利于将部分处理逻辑下沉到交换机流水线实现。

\begin{figure}[t]
\centering
% TODO 绘制EG/PT消息header格式图。
% 理想图像：bit-level字段布局（epoch/link_id/ttl/nonce/digest指针等），以及示意其走ISL的传播路径。
\includegraphics[width=0.92\linewidth]{fig/ch4_header.pdf}
\caption{协同控制消息的头部格式示意（EG与PT）}
\label{fig:ch4_header}
\end{figure}

\subsubsection*{（3）抑制型扩散协议（Suppressed Gossip Protocol）}
设节点$v$在epoch $\tau$内维护一个“证据缓存”$\mathcal{E}_{v,\tau}$，存储在ROI内观察到的EG消息摘要。对任意$(\tau,\texttt{link\_id})$，节点以固定时隙$I$执行一次抑制型扩散。其思想是：节点在$I$内随机选择一个发送时刻$t_{\text{send}}$；在$t_{\text{send}}$前若已监听到至少$k$条“等价证据”（同epoch同link\_id且digest高度相似），则抑制发送；否则在$t_{\text{send}}$发送聚合后的EG消息。通过随机化发送与冗余抑制，系统在保持传播速度的同时显著降低重复广播。

进一步地，为确保传播局部化，本章规定EG消息仅在满足“相关性门限”时才会被转发：节点$v$收到来自邻居$u$的EG消息$M$后，计算其与本地候选集合$\mathcal{A}_{v,\tau}$的重叠度：
\begin{equation}
\Omega(M,v) = \frac{|\mathcal{A}_{v,\tau}\cap \mathcal{A}(M)|}{|\mathcal{A}(M)|},
\label{eq:overlap}
\end{equation}
其中$\mathcal{A}(M)$表示消息中top-$n$ key集合。仅当$\Omega(M,v)\ge \theta_\Omega$或本地链路也处于拥塞态$c_{v,e}(\tau)=1$时，节点才会将该证据纳入缓存并参与后续转发。该机制利用“本地观测的一致性”来过滤无关扩散，是避免信令风暴的关键。

算法~\ref{alg:suppressed_gossip}给出了抑制型扩散协议的流程。与朴素gossip相比，其差异不在“传播能力”，而在于利用物理网络的局部性与低度数结构，将传播控制在ROI内，并用抑制机制避免同一证据在区域内反复回旋传播。

\begin{algorithm}[t]
\caption{抑制型邻居扩散协同（Suppressed Gossip Protocol）}
\label{alg:suppressed_gossip}
\begin{algorithmic}[1]
\REQUIRE epoch $\tau$，ROI半径$R$，抑制参数$k$，重叠阈值$\theta_\Omega$
\STATE \textbf{Trigger:} 若存在链路$e$使$c_{v,e}(\tau)=1$，构造本地EG候选消息$M_v$
\STATE 将$M_v$加入缓存$\mathcal{E}_{v,\tau}$，并进入抑制发送窗口$I$
\STATE 在窗口$I$内随机选择$t_{\text{send}}$
\WHILE{$t < t_{\text{send}}$}
    \STATE 监听邻居EG消息$M_u$
    \IF{$M_u$未去重（nonce未见过）且$M_u.\texttt{ttl}>0$}
        \STATE 计算$\Omega(M_u,v)$（可由digest快速近似）
        \IF{$\Omega(M_u,v)\ge \theta_\Omega$ \textbf{or} 本地存在拥塞链路}
            \STATE 将$M_u$合并入$\mathcal{E}_{v,\tau}$（top-$n$合并+digest并集）
        \ENDIF
    \ENDIF
\ENDWHILE
\STATE 统计窗口$I$内等价证据数量$cnt$
\IF{$cnt < k$}
    \STATE 生成聚合消息$\widetilde{M}_v = \texttt{Aggregate}(\mathcal{E}_{v,\tau})$
    \STATE 设置$\widetilde{M}_v.\texttt{ttl}\leftarrow R$，向所有邻居发送（不回发原入端口）
\ELSE
    \STATE 抑制发送（suppress）
\ENDIF
\end{algorithmic}
% TODO 补充Aggregate的细节：top-$n$合并策略（max/sum），digest并集策略，冲突处理等。
\end{algorithm}

\subsection{协同判定与推回缓解：从“受害链路”推回到“上游入口”}
\label{subsec:ch4_pushback}

\subsubsection*{（1）协同证据聚合与判定}
在ROI内，多个节点对同一攻击相关key会给出不同程度的本地评分$S_{v,\tau}(a)$。为形成网络级判定，本章定义协同聚合评分：
\begin{equation}
S^{\text{net}}_{\tau}(a) = \sum_{v\in \mathcal{R}_{\tau}}\omega_v \cdot \widetilde{S}_{v,\tau}(a),
\label{eq:net_score}
\end{equation}
其中$\mathcal{R}_{\tau}$为在epoch $\tau$内参与协同的ROI节点集合，$\widetilde{S}_{v,\tau}(a)$为归一化后的本地评分，$\omega_v$为权重。权重的设计根植于物理可观测量：拥塞越强、越接近受害链路的节点，其证据应更可靠。于是本章取：
\begin{equation}
\omega_v \propto \left(1+\lambda\cdot \texttt{cong\_level}_v\right)\cdot \exp(-\mu\cdot \texttt{dist}(v,e^\*)),
\label{eq:weight}
\end{equation}
其中$\texttt{dist}(v,e^\*)$表示$v$到受害链路$e^\*$的跳数距离，$\lambda,\mu$为超参数。在实现上，为避免复杂指数运算，控制面agent采用查表或分段线性近似完成权重计算；数据面只需提供$\texttt{cong\_level}$与邻接关系。

当存在$a$使$S^{\text{net}}_{\tau}(a)\ge \Theta_{\text{pb}}$时，系统对该key触发推回缓解；若多个key满足条件，则按$S^{\text{net}}_{\tau}(a)$排序取前$n_{\text{pb}}$个执行，以保证数据面策略表规模可控。

\subsubsection*{（2）推回令牌（PT）与逐跳传播}
pushback的关键是：不依赖全局路由逆推，而是利用\emph{逐跳可观测的上游贡献}实现“近似反向路径”传播。具体而言，受害链路附近节点$v$对key $a$计算其入端口贡献分布$\pi_{v,\tau}(a,p)$（式\eqref{eq:ingress_contrib}），选择贡献最大的前$r$个上游端口集合$\mathcal{P}^\uparrow_{v}(a)$，并向对应上游邻居发送PT令牌。令牌携带软状态生存期$T_{\text{life}}$与剩余推回跳数$R_{\uparrow}$，上游节点收到后立即在本地数据面安装短时效缓解规则（如队列降权或限速），并重复上述过程继续向其上游传播。由于LEO网络连接度低且拓扑变化在秒级\cite{icarus}，该逐跳机制在每个epoch内具有稳定语义；同时通过软状态过期可避免拓扑变化后产生“幽灵规则”。

算法~\ref{alg:pushback}给出了pushback传播流程。

\begin{algorithm}[t]
\caption{逐跳推回缓解（Hop-by-hop Pushback）}
\label{alg:pushback}
\begin{algorithmic}[1]
\REQUIRE 触发key集合$\mathcal{A}^{\text{pb}}_\tau$，推回半径$R_{\uparrow}$，上游分支数$r$
\FOR{每个$a\in\mathcal{A}^{\text{pb}}_\tau$}
    \STATE 在节点$v$生成PT令牌：$\langle a,\, \texttt{rank},\, \tau,\, R_{\uparrow},\, T_{\text{life}}\rangle$
    \STATE 计算上游贡献分布$\pi_{v,\tau}(a,p)$，选取前$r$大端口$\mathcal{P}^\uparrow_v(a)$
    \STATE 向$\mathcal{P}^\uparrow_v(a)$对应邻居发送PT
\ENDFOR
\STATE \textbf{Upon receiving PT at node }$u$:
\IF{PT未过期且$R_{\uparrow}>0$}
    \STATE 在本地数据面安装/刷新软状态规则：$\texttt{rank}(a)\leftarrow$ PT.\texttt{rank}，有效期$T_{\text{life}}$
    \STATE $R_{\uparrow}\leftarrow R_{\uparrow}-1$
    \STATE 基于$\pi_{u,\tau}(a,\cdot)$继续向上游前$r$大邻居传播
\ENDIF
\end{algorithmic}
% TODO 说明rank/限速动作的具体实现（match-action表项、队列映射方式、冲突解决）。
\end{algorithm}

\begin{figure}[t]
\centering
% TODO 绘制pushback传播示意图。
% 理想图像：受害链路附近节点识别a后，沿贡献最大的上游方向逐跳扩散PT；
% 标出R_up半径、r分支、以及不同节点安装的软状态规则生命周期。
\includegraphics[width=0.93\linewidth]{fig/ch4_pushback.pdf}
\caption{逐跳推回缓解传播示意：从受害链路向上游入口扩散短时效策略}
\label{fig:ch4_pushback}
\end{figure}

\subsection{信令开销与可实现性分析}
\label{subsec:ch4_overhead}

\subsubsection*{（1）ROI限制下的信令规模上界}
设星座网络的平均度数为$d$（典型LEO ISL结构中$d\approx4$）\cite{icarus}。对任意EG消息，TTL为$R$时，其在最坏情况下可触达的节点数不超过一个$d$叉树的$R$层节点规模：
\begin{equation}
| \text{ROI}(R) | \le 1 + d\cdot \frac{(d-1)^R - 1}{d-2},\quad d>2.
\label{eq:roi_bound}
\end{equation}
当$d=4$时，上界为$| \text{ROI}(R) | \le 2\cdot 3^R -1$。例如$R=4$时，最坏触达规模不超过161个节点，远小于Starlink Shell 1的1584节点总规模\cite{icarus}。因此，TTL本身已将协同传播限制为“受影响局部区域”，避免全网泛洪。

\subsubsection*{（2）抑制机制下的消息量与带宽开销}
在抑制型扩散中，节点在一个发送窗口内若监听到不少于$k$条等价证据即抑制发送，从而大幅降低重复广播。设ROI内有效参与节点数为$N_R$，每个节点每epoch至多发送一次聚合EG消息（多余将被抑制），则EG消息总数满足：
\begin{equation}
M_{\text{EG}}(\tau) \le N_R.
\label{eq:msg_count}
\end{equation}
进一步地，设每条EG消息大小为$L_{\text{EG}}$字节，则控制带宽开销上界为：
\begin{equation}
B_{\text{EG}} \le \frac{N_R\cdot L_{\text{EG}}}{\Delta}.
\label{eq:ctrl_bw}
\end{equation}
结合式\eqref{eq:roi_bound}与Starlink ISL 20~Gb/s容量级\cite{icarus}，当$R$取小常数（本章默认$R=4$）、$L_{\text{EG}}$处于KB级时，$B_{\text{EG}}$相对ISL带宽占比可控制在远低于$1\%$的量级。实验中我们进一步验证了控制开销与$R,k,\Delta$的敏感性（见第\ref{subsec:ch4_eval}节），并观察到抑制机制使得在攻击高峰期也不会出现信令风暴。

\subsubsection*{（3）数据面实现可行性}
实现层面，本章遵循“数据面负责常数复杂度快路径，控制面负责聚合与决策”的原则。数据面新增逻辑主要包括：
（i）为协同消息定义轻量header解析与TTL递减；
（ii）维护seen-set（可用Bloom Filter近似）用于消息去重；
（iii）维护key的入端口贡献统计$\text{bytes}_{v,\tau}(a,p)$；
（iv）执行pushback规则（队列降权/限速）并以软状态过期机制清理。
上述操作均可由可编程交换机的寄存器阵列与match-action表实现，且每包处理保持O(1)复杂度，满足线速约束。复杂的聚合评分（式\eqref{eq:net_score}）与抑制决策在星上agent完成，从而避免数据面计算负担过重。

\section{仿真与分析}
\label{sec:ch4_eval}

\subsection{实验设置}
\label{subsec:ch4_setup}

本章在Starlink Shell 1拓扑上进行评估，该拓扑包含1584颗卫星（72轨道面$\times$22颗/面），ISL为典型4链路连接结构，ISL带宽20~Gb/s，上下行链路容量为数Gb/s量级\cite{icarus}。为贴近真实网络背景流量，我们采用CAIDA真实trace构造良性流量，并按人口/城市对分布模型将流量映射到地面终端对（与SatShield的评估范式一致）\cite{satshield}。攻击流量生成采用ICARUS式LFA策略：攻击者利用公开轨道与拓扑信息选择bot对并计算使目标链路拥塞的流量分配\cite{icarus}；同时在更强对手模型下引入“低速化/多decoy/脉冲化/滚动目标”等策略，以检验协同机制对碎片化证据聚合能力。

评价指标分为三类：（1）防御效果：良性吞吐、端到端时延、受害链路拥塞持续时间、通信可达性；（2）检测质量：攻击相关key识别的Precision/Recall/F1（定义同第\ref{chap:ms_satshield}章式( \ref{eq:f1} )）；（3）协同开销：EG/PT消息数与字节开销、ROI规模、收敛时间（从首个拥塞触发到pushback生效的时间），以及由推回带来的“无效带宽占用”下降比例（对应式\eqref{eq:waste_bw}）。

\begin{table}[t]
\centering
\caption{对比基线设置（第四章）}
\label{tab:ch4_baselines}
\begin{tabular}{l|l}
\hline
方案 & 说明 \\
\hline
NoDefense & 无防御，FIFO/ECMP默认转发 \\
SatShield & 单星top-$k$过滤+队列调度缓解\cite{satshield} \\
MS-SatShield & 第\ref{chap:ms_satshield}章多签名增强，但无协同/无pushback \\
LinkScope & 基于探测/测量的LFA检测框架\cite{linkscope} \\
Woodpecker & 基于SDN/TE的LFA缓解（重路由/工程控制）\cite{woodpecker} \\
RADAR & 基于相关性分析的LFA检测与限速\cite{radar} \\
Ripple & 去中心化的可编程网络LFA防御框架\cite{ripple} \\
Mew & 面向动态与资源受限环境的LFA防御机制\cite{mew} \\
\textsc{CoopSatShield} & 本章方案：抑制型扩散协同+逐跳pushback \\
\hline
\end{tabular}
% TODO 若部分基线在LEO中需适配（例如控制环延迟/拓扑快照），请在正文说明适配策略。
\end{table}

表~\ref{tab:ch4_baselines}给出对比基线。需要说明的是：LinkScope、Woodpecker、RADAR等基线主要面向地面网络，其在LEO中面临控制环时延与拓扑快变问题\cite{satshield}；本章在实现对比时保持其核心思想不变，同时将控制消息延迟与拓扑快照约束显式纳入系统参数，以保证对比的物理一致性。

\subsection{结果与分析}
\label{subsec:ch4_results}

\subsubsection*{（1）推回缓解带来的网络级收益}
图~\ref{fig:ch4_waste_reduction}展示了在典型攻击场景下，各方案对“无效带宽占用”$W_{\text{waste}}$的影响。实验结果表明：SatShield与MS-SatShield虽然能够在受害链路处降低可疑流优先级，但攻击流在上游多跳链路上仍造成显著占用；而\textsc{CoopSatShield}通过逐跳pushback将抑制点推回至更靠近入口的位置，使攻击流在网络内部的平均承载跳数显著降低，从而带来$W_{\text{waste}}$的大幅下降。该结果直接验证了式\eqref{eq:waste_bw}的推回收益机理，并说明多星协同不仅提升检测覆盖，更能从网络资源角度提升整体鲁棒性。

\begin{figure}[t]
\centering
% TODO 绘制无效带宽占用对比图。
% 理想图像：柱状图或CDF。横轴方案，纵轴W_waste（Gb/s-hop 或 归一化比例）。
% 同时给出“平均承载跳数h”的对比（可作为副图或附表）。
\includegraphics[width=0.92\linewidth]{fig/ch4_waste_reduction.pdf}
\caption{推回缓解降低攻击流在网络内部的无效带宽占用}
\label{fig:ch4_waste_reduction}
\end{figure}

\subsubsection*{（2）良性吞吐与反应时间}
图~\ref{fig:ch4_throughput}给出了攻击开始后良性吞吐随时间变化。可以观察到，依赖中心控制环的方案（如Woodpecker、RADAR在LEO适配版本）由于统计收集与策略下发延迟，表现出更长的吞吐恢复时间；单星方案虽然反应较快，但由于抑制点靠近受害链路，仍会在上游链路形成排队并拖累端到端吞吐。相比之下，\textsc{CoopSatShield}在保持星上闭环快速性的同时，通过协同聚合与pushback使吞吐恢复更快、恢复后波动更小。

\begin{figure}[t]
\centering
% TODO 绘制吞吐时间序列：NoDefense/SatShield/MS-SatShield/CoopSatShield（可选加入RADAR/Woodpecker）。
% 标注：攻击开始时刻、首次拥塞检测时刻、pushback生效时刻、吞吐恢复时刻。
\includegraphics[width=0.92\linewidth]{fig/ch4_throughput.pdf}
\caption{良性吞吐随时间变化：\textsc{CoopSatShield}缩短恢复时间并降低吞吐下降}
\label{fig:ch4_throughput}
\end{figure}

\subsubsection*{（3）协同开销与“无信令风暴”验证}
本章最关注的工程风险之一是大规模网络中的信令风暴。图~\ref{fig:ch4_ctrl_overhead}展示了不同ROI半径$R$与抑制参数$k$下的控制开销。实验表明：在默认配置$R=4,k=\textbf{TODO}$时，EG消息总字节开销仅占ISL带宽的\textbf{TODO}\%以内，并且开销随$R$增长呈现可控趋势，远低于朴素gossip的线性增长。进一步观察消息数分布可知，抑制机制显著降低了“重复传播”比例，在攻击高峰期也未出现扩散爆炸。该结果验证了式\eqref{eq:roi_bound}与式\eqref{eq:ctrl_bw}的理论分析，并表明所设计的协同机制在千节点规模下具备可扩展性。

\begin{figure}[t]
\centering
% TODO 绘制控制开销与参数敏感性：
% (a) EG/PT消息数 vs R；
% (b) 控制字节开销(Bps) vs R；
% (c) 抑制参数k对消息重复率的影响。
\includegraphics[width=0.92\linewidth]{fig/ch4_ctrl_overhead.pdf}
\caption{协同控制开销与抑制机制效果：避免信令风暴的实验验证}
\label{fig:ch4_ctrl_overhead}
\end{figure}

\subsubsection*{（4）对碎片化攻击证据的鲁棒性}
最后，本章在“低速化/多decoy/脉冲化/滚动目标”等更强对手设定下评估协同机制的鲁棒性。结果表明，当攻击证据在单星视角下呈现碎片化时，MS-SatShield的本地检测Recall会出现下降；而\textsc{CoopSatShield}通过ROI内多点证据聚合显著降低了漏检率，并能更早触发pushback，从而在网络级指标（受害链路拥塞持续时间、通信可达性）上保持稳定。图~\ref{fig:ch4_robustness}给出了在不同bot规模与decoy规模下的F1与可达性对比。

\begin{figure}[t]
\centering
% TODO 绘制鲁棒性图：
% 横轴：bot规模或decoy规模；纵轴：F1/受害链路拥塞时间/城市对可达性；
% 对比：SatShield/MS-SatShield/CoopSatShield。
\includegraphics[width=0.92\linewidth]{fig/ch4_robustness.pdf}
\caption{在碎片化攻击设定下的鲁棒性：协同聚合提升检测覆盖并改善网络级效果}
\label{fig:ch4_robustness}
\end{figure}

\section{本章小结}
\label{sec:ch4_summary}

本章面向大规模低轨卫星网络的链路洪泛攻击，进一步从“单星本地闭环防御”推进到“多星协同在网计算与推回缓解”。针对LEO网络中控制环延迟高、拓扑快变、攻击自适应快的物理约束\cite{satshield,icarus}，本章提出\textsc{CoopSatShield}：以第\ref{chap:ms_satshield}章本地多签名检测为基础，将检测输出结构化为可协同的证据单元；设计抑制型邻居扩散协议，通过事件驱动、ROI限制与冗余抑制三重机制避免大规模网络中的信令风暴；并提出逐跳pushback机制，利用入端口贡献分布实现无需全局逆路由的推回缓解，将攻击抑制点推回至上游入口，从而降低网络内部无效带宽占用并提升良性吞吐保护。

实验结果表明，相较SatShield及典型地面LFA防御基线，本章方案在LEO场景下实现了更快的收敛与恢复、更低的控制开销以及更强的碎片化攻击鲁棒性，为全文“面向大规模低轨卫星的在网计算研究”提供了从\emph{单节点在网计算}到\emph{多节点协同在网计算}的完整技术链条。

	
	%% 参考文献部分
	% \nocite{*}% 为了便于在示例文档中展示参考文献而设，正式撰写时需要注释掉
	% \bibliographystyle{DissertUESTC}% 自v25.03.12版本后，参考文献风格文件由用户设定的论文类型选项自动确定，无需在此手动设置
	\bibliography{ref}
	
	% 附录起始位置
	\appendix
	\chapter{建模证明}
	
\end{document}